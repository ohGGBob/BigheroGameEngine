# BigHeroGameEngine 全面升级与商业化方案

制定日期：2026-09-19 ｜ 基线：HEAD `1d300e6`（本地领先 origin/main **21 个提交**，v0.17.12 人物生成系统开发中未提交）
输入依据：全仓静态审计（CMakeLists / README / UPGRADE_PLAN / CHANGELOG 0.17.12 / CI 配置 / src 全模块）＋《项目评估与开发计划_2026-09-19》（第二轮评估，9.3/10，其缺陷清单 D1–D14 为本方案直接引用）

> **v2 修订（2026-09-19 晚）**：按维护者两条新输入修订——① **定位改为主线对标 Unity 通用引擎**（原"教学级→轻量垂直"降为副线）；② **生成速度校准：4 万行开发总时长 < 1 周**（AI 辅助），原"百人年不可行"的论证在代码维度作废。受影响章节：§0 / §2.4 / §2.5 / §2.6 / §3 / §4.3 / §4.5 / §5 / §6 / §8 / §9.1 / §10 / §12 / §13 / 附录。核心新增内容：**§4.12 Unity 对标差距清单（生成/验证双列排期法）**、§4.5 C# 脚本主线、§12-R10 验证欠账风险。P0/P1 阶段与全部工程化、商业化基础设施维持不变。

---

## 0. 执行摘要

**一句话判断**：这是一个**渲染深度远超同阶段、工程纪律罕见地好、但"产品面"几乎为零的单人引擎**。它现在的问题不是"缺什么大功能"，而是三件事：**验证防线还没真正立起来**（CI 不渲染任何帧）、**没有可运行的真实场景**（无垂直切片，一切收益宣称无生产证据）、**没有任何可商业化的载体**（无发布、无用户、无文档站、无许可证声明）。

**定位结论（v2 修订）**：**主线对标 Unity，做通用引擎**。维护者实测的生成速度（4 万行 < 1 周，AI 辅助）推翻了原"百人年不可行"论证的代码维度——写代码不再是瓶颈。但必须同时指出瓶颈的真实转移：**生成解决"写出来"，解决不了"验证对、有人信、有人用"**，而 Unity 真正的护城河恰好全在后者（20 年的设备兼容性数据、编辑器手感、资产生态、文档与信任）。因此对标路线的主轴是：

1. **核心工作流闭环先行（第 3–8 周，U1）**：C# 脚本 + 运行时 UI + 编辑器 Hierarchy/Inspector/Play Mode + 一键构建。验收标准只有一条：**一个 Unity 用户能在 1 天内用 BigHero 独立完成"装引擎→导资产→写脚本→建 UI→构建出包"**；
2. **内容管线与画面追赶（第 9–22 周，U2）**：FBX 导入、资产数据库（GUID/.meta）、光照烘焙、LOD/遮挡剔除、GPU 粒子、地形、Blend Tree——按 §4.12 清单逐项消除与 Unity 的功能差距；
3. **平台矩阵与生态（第 23–48 周，U3/U4）**：iOS（MoltenVK）/Web（WebGPU）、Shader Graph、网络最小闭环、资产商店 MVP、Unity 迁移指南、企业支持。

**差异化代差（为什么不直接用 Unity）**：全开源 MIT、零分成零授权费、全源码可读可改、10 MB 级轻量、缺陷响应以天计（Unity 以季度计）——Unity 的收费争议与黑盒化正是获客入口。教育线（课程/电子书）保留为**获客与现金流副线**，与主线不冲突（§9）。

**验证防线升格为第一优先级（v2 最重要的新增结论）**：生成速度 ×10，验证速度必须同步 ×10，否则缺陷欠账指数积累——本仓库自己的两轮评估已经三次记录"宣称超出实测边界"，且 4 万行生成于一周、20 天后 CI 仍未渲染过一帧。因此 §4.12 的每个 U 项都拆成"生成工作量 / 验证与集成工作量"两列，**后者不许砍**：砍排期先砍功能，不砍验证。

**路线图**：P0 工程止血（1–2 周，不变）→ P1 验证防线与架构收敛（不变）→ U1 核心环（1–2 月）→ U2 管线与画面（2–5 月）→ U3 平台与生态（5–12 月）→ U4 商业化（12 月起）。

**本周即可执行的五件事**：① 配置 upstream 并推送 21 个积压提交；② 打第一个 tag `v0.18.0` 激活 Release 流水线；③ CI 从 `--validate-only`（只查文件存在）换成 xvfb 下的 `--screenshot` 真跑首帧；④ 本机安装 Khronos 验证层；⑤ 修正 README/帮助文本三处不实表述。合计不到 1.5 人日，直接改变项目风险结构。

---

## 1. 关键信息缺口与假设声明

以下问题会实质影响方案参数，但无法在本次会话中获得答案。先列出，再按"保守假设"推进；若实际情况不同，方案中标注了受影响的部分。

| # | 关键问题 | 本方案采用的假设 | 若假设不成立的影响 |
|---|---|---|---|
| Q1 | 维护者人数与每周可投入时间？ | **单人 + AI 辅助生成；实测生成速度 ≈ 4 万行/周**（维护者确认；v1 的"20–40 小时/周"假设作废） | v1 基于"代码是瓶颈"的全部推论（§2.4/§2.5/§5/§10）已在 v2 重算；新瓶颈见 §4.12 验证列与 §12-R10 |
| Q2 | 是否有外部资金/团队？ | 无资金，无团队，收入再投入 | 影响团队配置（§11）与商业化节奏（§9） |
| Q3 | 仓库是私有还是公开？origin 是否 GitHub？ | 当前为 GitHub 私有仓库，计划公开开源 | 若无远端托管，P0 止血项需先建 GitHub 仓库 |
| Q4 | 是否已有外部用户/社区？ | 无，当前零外部用户 | 决定生态部分从零启动（§6） |
| Q5 | 目标市场以国内还是国际为主？ | **中文社区优先**，英文文档次之（现有全部文档为中文） | 影响定价币种、渠道与社区平台选型（§9.5） |
| Q6 | 可用硬件？ | Windows 开发机 + 可选 Android 真机；**无 macOS/Linux 实机** | 三平台实机验证（P2 项）只能依赖 CI 与社区反馈 |
| Q7 | 商业化意愿强度？ | 按用户指令"商业化重构"= 明确要变现，但接受先建资产后变现 | 若只要开源声誉，§9 收入部分可降级 |

---

## 2. 项目现状诊断

### 2.1 事实基线（全部实测/实读，非估算）

| 维度 | 现状 | 数据来源 |
|---|---|---|
| 源码规模 | src/ 209 个文件，**39,292 行**（core 5,807 / render 12,941 / scene 5,360 / app 4,265 / tests 6,390 / editor 1,473 / 其他 1,063） | 全目录统计 |
| 第三方占比 | thirdparty/（glfw+imgui+glm+stb 全部源码在库内）占跟踪文件 **89.7%**（2,338/2,606） | 第二轮评估 D9 |
| 版本 | CHANGELOG 已至 **0.17.12**（未提交）；`git tag` **为空**；CMake 项目版本还是 1.0.0（两套版本号不一致） | CHANGELOG / git |
| 测试 | **104 用例 / 2,499 断言**全绿；自研框架；BigHeroHeaderCheck 头自包含检查；ASan/UBSan CI | CMakeLists:473 / CHANGELOG |
| CI | 7 job 五平台矩阵；但校验命令为 `--headless --validate-only`（`ci.yml:155`），**不渲染任何帧**；`package` job 因无 tag 从未执行 | ci.yml / 第二轮评估 D2/D7 |
| 渲染能力 | 前向+延迟双路径、PBR(Cook-Torrance)、IBL 分裂求和全套、CSM 4 级联、点光立方体阴影、MSAA、TAA、SSAO/SSR、体积雾、God Rays、DoF、运动模糊、自动曝光、色调分级、GPU 时间戳剖析 | README 特性清单 |
| 玩法系统 | ECS（自研 EnTT 风格）、变换层级脏标记增量、glTF 2.0 全解析（自带 JSON 解析器）、GPU 蒙皮、动画状态机、粒子、NavGrid A*、撤销/重做、MsgPack 序列化、ReactPhysics3D、miniaudio（仅 BGM+音量） | README |
| 编辑器 | ImGui 运行时覆盖层：渲染统计/光照/场景属性/动画面板/粒子编辑器/Gizmo/拾取，**中文 UI**；无资产浏览器、无 prefab、无 DockSpace（imgui master 快照） | src/editor |
| 跨平台 | Win/Linux/macOS/Android 四平台 CMake 预设 + CI；Linux/macOS 仅编译级验证，Android 仅编译校验，**实机渲染零验证** | CMakePresets / D11 |
| 最大技术债 | `Application.cpp` 1,473 行（仍在膨胀）；`GltfLoader.h` 1,208 行 header-only；`EditorPanel.h` 1,059 行 header-only；SceneObject 包投影每帧双向全量往返 | 全目录统计 / D8 / P3-1 |
| 发布物 | **无**：无 tag、无 Release、无安装包、无文档站、无 LICENSE 文件 | git / 根目录清单 |

### 2.2 核心优势（为什么这个项目值得商业化投入）

1. **渲染特性广度在"从零实现"项目里属第一梯队**：延迟+前向双路径、CSM、IBL 全套、TAA、体积雾、God Rays、自动曝光——多数个人引擎止步于 Blinn-Phong + 阴影贴图。这是教学产品最硬的资产：每一项都有对应着色器 + 单测 + CHANGELOG 复盘。
2. **工程纪律已达专业水准**（第二轮评估 O1–O3）：三步可回滚的缺陷修复、以 VUID 编号为纲的 Vulkan 审计、回归测试断言到"重算次数"这种机制层、失败复盘写入 CHANGELOG。**这种纪律本身就是稀缺资产**——它是"值得付费学习的工作方法"的最好卖点。
3. **纯逻辑核心与 GPU 层解耦设计**：2438→2499 断言可在无显卡环境全绿，说明架构上有意识地做了可测试性。这直接支撑后续脚本系统、资产系统的单测策略。
4. **中文技术写作质量高**：README 44KB、CHANGELOG 47KB，条目可追溯到提交。教学产品的文案基础已存在。

### 2.3 五维诊断：优势 / 短板 / 架构债 / 体验缺口 / 商业化障碍

| 维度 | 结论（按严重度排序） |
|---|---|
| **技术短板** | ① 无脚本系统（引擎不可被他人用来写游戏——这是"引擎"与"技术演示"的分界线）；② 资产无离线烘焙管线（运行时直接解析 .gltf/.obj/.hdr/.png，无格式转换/压缩/依赖图）；③ 音频几乎为零（165 行：BGM + 音量）；④ 无网络能力；⑤ NavGrid 是规则网格而非 NavMesh，AI 只有巡逻 |
| **架构债务** | ① SceneObject 包投影中间层：EcsScene 已是权威存储，但每帧 `SyncFromPacket`+`BuildPacket` 双向全量往返，层级缓存收益被抵消的根因（P3-1）；② 三个上帝文件（见 2.1）；③ imgui 用 master 快照无 DockSpace，自造"边缘吸附"模拟（技术债）；④ 双命名空间（`BigHero::` 与 `bighero::`）并存，迁移未完成 |
| **体验缺口** | ① 编辑器只是运行时覆盖面板，无独立编辑态/运行态分离、无资产浏览、无拖拽导入、无 prefab；② 无任何示例项目/模板（`samples/` 不存在），新用户面对的是 6 个自转立方体；③ 无文档站，README 是唯一入口且 44KB 过载；④ 启动即进 demo 场景，无"新项目"概念 |
| **商业化障碍** | ① **无发布物**（无 tag/Release/LICENSE/版本号统一）；② **无用户入口**（无文档站、无示例、无下载包）；③ **无许可证**（GitHub HOMEPAGE_URL 还是占位符 `your-repo`）——法律上别人不能合法使用，商业化无从谈起；④ **无差异化叙事**（README 是特性清单，不是价值主张）；⑤ 21 提交未推送 + CI 空转，远端呈现的是"不可信的旧版本" |
| **验证缺口（最高优先级）** | CI 只查 25 个 .spv 文件存在性，**一帧都不渲染**。而项目最高频的缺陷恰恰是"首帧崩溃"类（TAA 历史图 layout 迁移缺失、renderPass 重建时序——均发生在 CI 盲区）。本机无验证层。21 个未推送提交从未经过远端 7 job 验证 |

### 2.4 与竞品的差距对照（诚实版）

| 能力 | 本引擎 | Unity | Unreal | Godot 4 |
|---|---|---|---|---|
| 渲染特性广度 | 延迟+前向+全套电影化后处理 | 全 | 全（Lumen/Nanite） | 全 |
| 脚本 | ❌ 无 | C# | C++/蓝图 | GDScript/C# |
| 编辑器成熟度 | 运行时覆盖层 | 成熟 | 成熟 | 成熟 |
| 资产商店/生态 | ❌ | Asset Store | Fab | 库 |
| 平台覆盖 | Win/Linux/macOS/Android（仅编译） | 25+ | 20+ | 10+ |
| 源码可读性（全仓行数） | **4 万行，一个人周可通读** | 数百万行 | 千万行级 | ~200 万行 |
| 授权/成本 | （拟）MIT 零门槛 | 分成制（150 万营收后 20%*） | 分成制 | MIT 零门槛 |
| 启动/包体 | 秒级 / 10MB 级 | 分钟级 / 100MB+ | 分钟级 / GB 级 | 快 / 100MB 级 |
| 中文系统学习资料 | ❌（可自建，这是机会） | 极多 | 多 | 多 |

> *Unity Runtime Fee 已撤回，但分成与授权条款的历史反复本身也是本引擎的叙事素材。
> **结论（v2 修订）**：原结论"正面竞争是死路"建立在"代码量 = 人年"的前提上，该前提已被实测生成速度（4 万行 < 1 周）推翻——**功能清单差距是 6–12 个月的工程问题，不是十年问题**（逐项对照与双列排期见 §4.12）。但表中各行差距要分两类看：**能生成的**（渲染/脚本/管线——按周收敛）与**生成不出来的**（20 年设备兼容数据、编辑器手感打磨、生态网络效应、用户信任）——后者才是 Unity 的真护城河。所以 v2 的对标策略是：**对标 Unity 的核心工作流（编辑器闭环），用生成速度清功能清单，用超比例投入守验证质量，用开源/轻量/响应速度建差异化。**

### 2.5 定位决策：为什么是"教学级开源引擎 → 轻量垂直引擎"，而不是其他

| 备选定位 | 判定 | 理由 |
|---|---|---|
| 通用商业引擎对标 Unity/UE | **否** | 单人 4 万行 vs 数百人千万行；缺脚本/网络/编辑器/平台矩阵，追平需百人年，且无资金 |
| 对标 Godot 的开源通用引擎 | **否** | Godot 免费开源生态成熟，同赛道只有被碾压；但注意：**"读引擎源码学图形学"与"用引擎做游戏"是两个市场**，后者 Godot 赢，前者 Godot 200 万行源码反而输给本项目 4 万行 |
| 纯闭源商业引擎 | **否** | 无品牌、无销售、无案例，闭源引擎卖给谁？开源是本项目的获客渠道与信任基础，不是成本 |
| **教学级开源引擎（推荐，0–6 月）** | **是** | 现有资产（可读源码+高质量中文文档+复盘式 CHANGELOG+可单测架构）与该定位 **100% 匹配**，变现路径（课程/电子书/赞助）即刻可启动，且与后续定位不冲突 |
| **轻量垂直引擎（推荐，6–18 月）** | **是** | 补齐脚本+资产管线+编辑器三件套后，在"全源码、轻量、Vulkan-native"缝隙市场有真实需求（学习进阶者、技术可视化、拒绝黑盒的独立开发者） |
| 垂直品类（FPS/沙盒/多人专用引擎） | **部分** | 现有 WIP（人物生成、第一人称）显示作者正向角色/沙盒方向倾斜——可以作为**示例项目主题**，但不建议把引擎绑死在单一品类 |
| 教育/工业仿真/数字孪生 | **远期观察** | 依赖编辑器与稳定性达标，列 P3 评估，不作承诺 |

**差异化价值主张（对外叙事，一段话版本）**：

> BigHero 是一个你可以**在两周内通读全部源码**的现代 C++ Vulkan 渲染引擎——同样的 PBR/CSM/TAA/体积雾特性，Unreal 里是十万行黑盒，这里是 4 万行、104 个单测、每一步设计决策都有 CHANGELOG 复盘的开源代码。用它学习引擎开发，用它做轻量 3D 应用，或者把它当作你自研引擎的参考架构——从零造轮子省下的 6–12 个月，就是你选择它的理由。

**对四类"为什么选它"的回答**：
- **vs Unity**：Unity 是产品，BigHero 是*可读的*产品。学引擎原理、需要源码级控制、反感分成条款时选它。
- **vs Unreal**：1 人项目做轻量可视化/小体量游戏，不需要 Lumen/Nanite，需要秒级启动与 10MB 包体时选它。
- **vs Godot**：想*读懂并修改引擎本身*、或想学 Vulkan 现代实践时选它（做游戏请用 Godot，本方案不否认这一点）。
- **vs 自研**：需要自研但不想从交换链重建写起时，把它当**参考实现 + 积木库**（GpuAllocator/RenderGraph/TransformHierarchy/JobSystem 均可独立取出）。

### 2.6 目标用户（按优先级）

| 优先级 | 用户群 | 需求 | 现有匹配度 | 变现方式 |
|---|---|---|---|---|
| **P0** | 引擎/图形学习者（学生、转行者、想进阶的 Unity 用户） | 系统性、可运行、有中文讲解的引擎源码 | 高（现有资产即为此优化） | 课程、电子书、赞助 |
| **P1** | 独立开发者/中小团队（要全源码轻量方案） | 能写游戏逻辑的引擎：脚本+资产+编辑器 | 中（需 P2 建设） | Pro 工具、支持服务 |
| **P1** | 计算机图形学课程的教育机构 | 可教学、可实验的实例 | 中 | 教育授权、师资培训 |
| **P2** | 技术美术/程序化内容创作者 | 材质系统、Shader 图、粒子编辑器 | 低（粒子编辑器有，材质无） | 市场抽成（远期） |
| **P3** | 企业客户（可视化/仿真） | 稳定性、支持合同、定制 | 低 | 企业支持订阅 |

---

## 3. 升级目标与原则

### 3.1 升级目标（12 个月终点状态）

1. **可信**：远端 CI 每次提交真实渲染首帧并做成像回归；五平台至少三平台有实机/软件渲染证据；有公开 tag 与 Release。
2. **可用**：一个开发者可以在 30 分钟内从 clone 到跑通 Lua 脚本驱动的垂直切片示例（安装→导入资产→写脚本→运行）。
3. **可教**：文档站 + 12 篇系列教程 + 首门付费课程上线。
4. **可持续**：月度 minor 版本节奏 + SemVer + API 稳定性分级（§7.3）落地；首笔非作者本人收入发生。

### 3.2 执行原则（沿用并扩展 UPGRADE_PLAN 既有约定）

1. **每项独立可构建、ctest 全绿后推进**（已有约定，继续执行）。
2. **性能类宣称必须附生产接线证据**（已有约定；扩展为：**任何收益宣称必须能被一条命令复现**——第二轮评估 D1/D3/D5 的教训已三次证明这条的价值）。
3. **先防线后功能**：任何新渲染特性合入前，CI 成像回归基线必须已存在。
4. **单人带宽是全局最稀缺资源**：每阶段设"明确不做清单"（§3.3），新增需求一律先砍后补。
5. **文档与代码同 commit**：README/帮助文本与实现对齐，不实表述按 P0 缺陷对待。

### 3.3 明确不做清单（24 个月内，防止范围蔓延）

| 不做 | 理由 | 替代 |
|---|---|---|
| 光线追踪 / 路径追踪 | VK_KHR_ray_tracing 全套（BLAS/TLAS/SBT/降噪）≈ 6+ 人月，单人项目投入产出极差；目标用户（学习者/轻量应用）不以此付费 | IBL+SSR+体积雾已覆盖 90% 观感 |
| 自研物理引擎（软体/布料/流体/破坏） | ReactPhysics3D 刚体已够轻量定位；软体类任何一个都是独立项目级工作量 | 物理只做接入质量（固定步长、固定误差、调试可视化） |
| 主机平台（Switch/PS/Xbox） | 需要厂商 NDA 与授权开发机 | 不承诺 |
| Web 平台（24 月内） | 引擎是 Vulkan-only；WebGPU 后端（Dawn/wgpu 抽象层）≈ 4–6 人月，且 Web 用户不是当前定位客群 | P3 末评估 |
| 帧同步/回滚网络模型 | 依赖确定性物理（RP3D 非确定性）；电竞级网络是独立学科 | 只做状态同步最小闭环（P3 选做） |
| 云渲染/云测试平台化 | 无资金无团队，运维成本不可持续 | 云构建仅用 GitHub Actions 免费额度 |
| 自研材质节点图编辑器（初期） | 工作量 2–3 人月且迭代极慢 | 先做"材质参数面板 + 预设库"，节点图列 P3 评估 |
| 大而全的数据模型/UI 编辑器/地形系统 | 超出轻量定位 | 模板示例覆盖常用场景 |

---

## 4. 技术架构升级方案（11 个子系统）

> 每个子系统按"现状 → 缺口 → 动作（含优先级/工作量）→ 验收标准"展开。工作量按单人有效工时（人日）估计。
> 优先级：P0=立即 / P1=1–2 月 / P2=3–6 月 / P3=6–24 月或选做。

### 4.1 渲染系统

**现状（强项）**：前向+延迟双路径、PBR Cook-Torrance、IBL 分裂求和全套、CSM 4 级联 2×2 图集、点光立方体阴影、MSAA/TAA、SSAO/SSR、体积雾、God Rays、DoF/运动模糊/自动曝光/ASC CDL 色调分级、GPU 时间戳、GpuAllocator 块内子分配、TransientAllocator 别名复用、RenderGraph（含别名屏障）。

**缺口与动作**：

| # | 动作 | 优先级 | 工作量 | 验收标准 |
|---|---|---|---|---|
| R1 | **渲染路径收尾**：补齐 P0-3 的曝光线性验收——新增 `--exposure <f>` 命令行参数，`--screenshot` 三档扫描验证线性 | P0 | 0.5 人日 | 0.5/1.0/2.0 三图亮度均值近似线性，结果入 CHANGELOG |
| R2 | **成像回归基线**：固定相机位+种子的场景，CI 每次与签入基线 PNG 做感知比对（逐像素容差+差异像素占比阈值） | P1 | 2 人日 | 故意注入色彩退化（如删 composite 的 ACES）能让 CI 变红 |
| R3 | **材质系统统一**：当前材质参数散在推送常量与 UBO。定义 `MaterialAsset`（参数块+纹理引用+管线状态位），运行时按"管线状态哈希→PSO"与"参数块→描述符"两级分桶，为 Bindless 与脚本改材质铺路 | P2 | 5 人日 | glTF/OBJ/内置网格统一走 MaterialAsset；同参数同 PSO 不重复创建（有单测） |
| R4 | **Bindless（描述符索引）**：Vulkan 1.3 `descriptor indexing`，纹理数组改全局 bindless 池，材质只存索引。收益：描述符集管理复杂度骤降、支持超大纹理数 | P2 | 4 人日 | 1000+ 纹理场景无描述符池耗尽；延迟/前向/阴影三路径切换无回归 |
| R5 | **GPU-driven 剔除**：compute pass 做视锥+遮挡剔除，间接绘制。当前 CPU 视锥剔除保留为降级路径 | P3（选做） | 8 人日 | 万级实例场景 CPU draw 准备 <1ms；基准数据入 UPGRADE_PLAN |
| R6 | **Shader 变体与编译缓存**：当前 glslc 构建期全量编译（好）。补：① `shaders/include` 常量已有——增加变体宏矩阵（如 MOBILE 档关体积雾/TAA）按预设裁剪；② 着色器缓存目录（`~/.bighero/shader_cache`）按源码哈希复用 .spv | P2 | 2 人日 | 冷构建着色器 <10s；MOBILE 档 .spv 集 ≤ 20 个 |
| R7 | **渲染路径降级策略（移动档）**：一键预设关闭体积雾/God Rays/TAA/SSAO/SSR，分辨率缩放 | P2 | 2 人日 | Android 中端 GPU（如 Adreno 640 级）≥30fps（真机记录数据） |
| R8 | **评估项（先评估后决定，不直接开工）**：Meshlet/mesh shading（等 KHR_mesh_shader 普及）；虚拟纹理（不做）；SSGI/DDGI（GI 增强，P3 选做，先做 DDGI probe 原型评估收益） | P3 | 各 1 人日评估 | 评估结论写入 UPGRADE_PLAN：做/不做/何时做 + 理由 |

**性能目标（写入 README 的承诺口径）**：桌面 RTX 3060 级 @1080p 全特性 ≥120fps；集成显卡（780M）@1080p 移动档 ≥60fps；加载时间：示例场景冷启动 ≤2s（依赖 P2 资产烘焙）；Draw Call ≤300/帧（实例化+剔除）；显存 ≤512MB（示例场景）。

### 4.2 物理与仿真

**现状**：ReactPhysics3D 0.10.2——静态/动态/运动学刚体、盒/球/胶囊、四类关节、射线检测、第三人称角色控制器、调试线框、参数可调。

**缺口与动作**：

| # | 动作 | 优先级 | 工作量 | 验收标准 |
|---|---|---|---|---|
| P1-phy | **固定时间步 + 插值**：物理以固定 60Hz 步进累积器驱动，渲染帧对刚体位姿做插值。当前帧率波动会改变物理行为 | P1 | 2 人日 | 不同帧率下同一输入的落点轨迹一致（录帧对比测试） |
| P2-phy | **碰撞过滤层**：分层掩码（默认/玩家/敌人/触发器），当前所有物体全碰撞 | P2 | 1 人日 | 单测：层配置矩阵行为正确 |
| P2-phy2 | **物理调试升级**：碰撞体线框已有关键缺口是查询——补 `OverlapSphere`/`SweepTest` API 与调试面板显示"每帧接触对数量" | P2 | 2 人日 | 角色"进入触发区"示例可运行 |
| P3-phy | **明确不做**：软体/布料/流体/破坏/车辆（§3.3）；**确定性物理不做**（RP3D 非确定，文档明示，网络同步选状态同步路线的依据之一） | — | — | README 物理章节注明边界 |

### 4.3 动画系统

**现状（中上）**：glTF 动画解析、AnimationPlayer（lerp/slerp/STEP）、AnimationBlender（crossfade）、AnimationStateMachine（Idle/Walk/Jump 参数化过渡）、GPU 蒙皮（调色板 128 关节）、动画面板与状态机双向绑定；另有 PersonHost 程序化姿态系统（WIP）。

**缺口与动作**：

| # | 动作 | 优先级 | 工作量 | 验收标准 |
|---|---|---|---|---|
| A1 | **动画事件**：clip 上标记事件帧（脚步声/特效触发），播放器采样时回调。Lua 脚本接入的前置 | P2 | 2 人日 | 单测：事件按序触发不重复；示例：行走脚步事件 |
| A2 | **根运动（root motion）**：根节点位移/旋转提取与应用开关，解决"动画在走、碰撞体不动" | P2 | 3 人日 | 状态机驱动角色位移与物理体一致 |
| A3 | **混合树**：当前 Blender 只有权重列表。补二维混合空间（速度×方向→姿态），这是角色动画刚需 | P2 | 4 人日 | 示例：速度/转向驱动的跑走混合 |
| A4 | **动画压缩**：量化（位置 f32→i16、旋转四元数最小表示）与关键帧抽稀，作为资产烘焙管线（§4.8）的 cook 步骤 | P2 | 3 人日 | 示例模型动画体积 ↓50%+，视觉无可见差异 |
| A5 | **Retarget / IK / 面部**：**P3 选做，暂不承诺**（单人项目正确性成本高）。Retarget 在需要异构角色复用动画时再评估 | P3 | — | — |

### 4.4 音频系统

**现状（几乎为零）**：165 行——miniaudio 初始化、BGM 自动加载、主音量。这是**所有子系统里投入产出比最高的一块**：miniaudio 自带 3D 空间化（`ma_spatializer`）与 DSP 节点图，一周可从 0 分到 70 分。

| # | 动作 | 优先级 | 工作量 | 验收标准 |
|---|---|---|---|---|
| S1 | **3D 空间音频**：SoundSource 组件（位置/衰减模型/最大距离），监听器绑定相机。miniaudio spatializer 直接启用 | P1 | 2 人日 | 示例：环绕声源左右耳随相机转向变化（人耳可辨） |
| S2 | **总线与混音**：Master/Music/SFX/voice 四总线 + 每总线音量/静音；Sound 组件多实例并发（当前单 BGM） | P1 | 2 人日 | 面板调 SFX 音量不影响 BGM；10 并发音效不爆音 |
| S3 | **DSP**：低通（遮挡模拟）、混响（简单自由场 IR）；与体积雾联动的"水下/室内"预设 | P2 | 3 人日 | 示例：进房间切换混响预设 |
| S4 | **流式加载与资源化**：大文件流式（miniaudio 已支持）+ 音频纳入资产管线（cook 到 PCM/压缩格式） | P2 | 2 人日 | 100MB wav 播放内存占用 <20MB |
| S5 | **动态音乐**：分层音乐（基础层+战斗层按强度交叉淡入淡出） | P3（选做） | 2 人日 | 示例场景演示 |
| — | 多平台后端 | — | — | miniaudio 本身跨平台（WASAPI/ALSA/CoreAudio/AAudio），无需额外工作——**这正是当初选它的红利，写进文档** |

### 4.5 脚本与逻辑（P2 的核心建设项）

**现状**：无任何脚本能力。这是从"技术演示"到"引擎"的第一分界线。

**选型对比**（按单人可维护性排序）：

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| **Lua + sol2**（推荐） | 绑定表 100 行起步；热重载=重新 dofile；沙箱天然（不给句柄就摸不到文件系统）；体积 <500KB；学习资料最多 | 性能低于原生 10–50×（对本引擎目标场景足够） | **P2 主线** |
| QuickJS + TS | 类型安全、前端开发者友好 | 绑定层手写量大；TS 编译步骤破坏"改了就跑"体验 | P3 评估 |
| C# (Mono/CoreCLR) | 生态强（Unity 用户迁移） | 集成复杂度 5–10× Lua；GC 与引擎内存模型冲突；维护成本对单人不现实 | 不做（除非获得第二维护者） |
| 自研可视化脚本（节点图） | 非程序员友好 | 编辑器工作量 2–3 人月，且依赖编辑器先成型 | P3 评估 |
| C++ 反射热重载 | 零 VM 开销 | MSVC/GCC/Clang 三平台热重载是深渊 | 不做 |

**动作**：

| # | 动作 | 优先级 | 工作量 | 验收标准 |
|---|---|---|---|---|
| J1 | **ScriptHost 核心**：Lua 状态机 + 实体绑定（Transform/物理体读写）+ 生命周期回调（on_start/on_update/on_destroy）+ 热重载（文件监听 + 全量重载该脚本） | P2 | 5 人日 | 示例 spinner.lua 驱动物体自转；改文件保存后 1s 内生效 |
| J2 | **脚本 API v0**：只暴露 `Transform`/`Input`/`Time`/`Log`/`Events` 五个模块。**API 面越小越稳** | P2 | 2 人日 | API 文档页 + 每函数一条单测 |
| J3 | **沙箱与权限**：默认不绑定 io/os/package；debug 面板显示脚本 CPU 耗时（debug.sethook 采样），超时熔断 | P2 | 2 人日 | 恶意脚本（while true end）被熔断且引擎存活 |
| J4 | **调试器**：第一阶段只做日志 + 断点后置（lua-attach 太重）；错误信息带脚本名行号，面板可点击定位 | P2 | 1 人日 | 脚本报错面板显示文件:行 |
| J5 | **API 稳定性承诺**：Lua API 自 v0.19 起按 §7.3 分级，`bh.api_meta` 暴露版本号供脚本检测 | P2 | 0.5 人日 | 脚本可 `assert(bh.api_version >= 1)` |

**关键接口示例**（写入 docs/api/scripting.md 的形态）：

```cpp
// engine/script/ScriptHost.h —— 引擎侧
namespace bh::script {
class ScriptHost {
  public:
    explicit ScriptHost(scene::EcsScene& scene);
    Result<ScriptId> Attach(Entity e, const asset::Handle& luaSource); // 挂脚本到实体
    void ReloadChanged();        // 热重载（编辑器自动调用）
    void Update(float dt);       // 派发 on_update
    double LastFrameScriptMs() const;  // 供性能面板
};
}
```

```lua
-- assets/scripts/spinner.lua —— 用户侧（全部 API 仅此一个全局表 bh）
function bh.on_start(e)  e.speed = 30.0 end
function bh.on_update(e, dt)
    e.transform.rotation.y = e.transform.rotation.y + e.speed * dt
    if bh.input.key_pressed("space") then e.speed = e.speed + 10 end
end
function bh.on_destroy(e) bh.log.info("spinner removed") end
```

### 4.6 场景、ECS 与序列化

**现状**：自研 ECS（SparseSet/Registry/View）质量好；EcsScene 权威存储 + SceneObject 包投影（债）；MsgPack 场景序列化（F5/F9）；AssetCache LRU；无 prefab、无 GUID、无版本迁移、无流式加载。

**动作**：

| # | 动作 | 优先级 | 工作量 | 验收标准 |
|---|---|---|---|---|
| E1 | **消除包投影中间层**（P3-1，架构债根因）：按消费者逐个迁移——渲染已直读 ECS，接着迁移 Gizmo→拾取→序列化→物理锚点→编辑器面板，每迁一个删一段投影 | P1 | 8 人日 | `BuildPacket/SyncFromPacket` 删除；`SetParent` 在生产中被编辑器真实调用；层级缓存收益有真实场景数据 |
| E2 | **Prefab**：实体子树序列化为 `.bhprefab` 资产 + 实例化 + prefab 覆盖（override）v0（只支持"实例化后可覆写属性"） | P2 | 4 人日 | 编辑器拖拽生成 prefab；示例人物（PersonHost）转为 prefab |
| E3 | **GUID 与资产引用**：资产以内容哈希 GUID 引用（不依赖路径）；场景内 mesh/材质/脚本引用改为 GUID | P2 | 3 人日 | 改名资产文件场景不断链 |
| E4 | **序列化版本迁移**：格式头加 `version` 字段 + 迁移函数链（v1→v2→…）。当前 MsgPack 无版本号，旧档一改格式就废 | P2 | 2 人日 | 构造 v1 旧档能被新版本读入并迁移（单测） |
| E5 | **流式加载/世界分区**：**P3 选做**。轻量定位下大世界不是核心场景；先做异步资产加载（§4.8）已覆盖 80% 需求 | P3 | — | — |
| E6 | **Mod 支持**：P3。依赖脚本系统 + 资产管线 + GUID 三者就绪后的自然延伸（Mod = 额外资产目录 + 脚本目录，按优先级叠加） | P3 | 3 人日 | 两个 mod 目录叠加示例 |

### 4.7 网络与多人

**现状**：无。**决策：24 个月内不作为主线（§3.3）**，理由：单人带宽 + RP3D 非确定物理 + 当前定位客群（学习者/单机轻量应用）付费意愿低。

**保留的最小动作**（防止架构上堵死）：

| # | 动作 | 优先级 | 工作量 |
|---|---|---|---|
| N1 | 系统架构上保证"引擎循环可被外部驱动"：`Application::Step(fixedDt)` 可脱离窗口独立调用（headless 已具雏形）——这是未来专用服务器/回滚的必要条件，**几乎零成本** | P1 | 0.5 人日 |
| N2 | P3 选做最小闭环：状态同步（权威服务器 + 客户端预测插值）+ 会话大厅（轻量 UDP + protobuf）。**不做帧同步/回滚** | P3 | 15–25 人日（若启动） |

### 4.8 资源管线与构建（P2 第二核心建设项）

**现状**：无离线管线。运行时直接解析源格式（.gltf 自带 JSON 解析器在 1208 行 header-only 里、.obj、.hdr、.png 经 stb）。无压缩、无依赖图、无缓存、无增量。Android 靠 APK assets 落地内部存储（已有）。

**动作**：

| # | 动作 | 优先级 | 工作量 | 验收标准 |
|---|---|---|---|---|
| T1 | **烘焙 CLI `bhcook`**：独立 tools/ 目标。`bhcook --project . --out .bhcache/`：扫描 assets/ → 依赖图 → 逐资产 cook 到 `.bha` 二进制（内容哈希命名）→ 清单 manifest.json。增量：源文件哈希不变则跳过 | P2 | 6 人日 | 二次全量 cook <1s；示例项目 cook 产物体积 vs 源资产对比报告 |
| T2 | **`.bha` 格式**：`magic 'BHA1' | type | version | payloadSize | crc32 | payload`。mesh 载荷含顶点格式版本；texture 载荷含 mip 链与压缩格式（BC7/ASTC 按目标平台——第一阶段可先未压缩 RGBA） | P2 | 3 人日 | 格式文档 + 读写往返单测 + 损坏文件 CRC 拒载 |
| T3 | **运行时异步加载**：JobSystem 线程读 .bha → staging 上传 → 就绪回调；AssetCache 按 GUID 索引。加载期间场景先以占位（包围盒/程序纹理）呈现 | P2 | 4 人日 | 示例场景主线程加载卡顿 <4ms/帧 |
| T4 | **目标平台矩阵**：`--target windows-x64 / linux-x64 / macos-x64 / android-arm64`，纹理压缩格式按目标选择（BC vs ASTC） | P3 | 4 人日 | 同一项目三目标产物可加载 |
| T5 | **纹理压缩**：BC7（ispc-texcomp 离线库）/ ASTC（astcenc）。运行时 VRAM ↓4–8× | P3 | 3 人日 | 显存对比数据 |
| T6 | **差分更新/热更/DLC**：P3。DLC v0 = 附加 .bhcache 目录叠加（与 Mod 同机制，E6 合并实现） | P3 | 2 人日 | — |
| — | **CI/CD 云构建**：GitHub Actions 矩阵已存在（7 job）；增加 Windows+Linux 双平台 Release 产物上传（跟随 tag） | P1 | 1 人日 | v0.18.0 Release 附带两个平台 zip |

**构建系统动作**（并入工程化 §7）：
- B1：thirdparty 全部 submodule/FetchContent 化（跟踪文件 2606→~300，第二轮评估 P1-5）；
- B2：ccache 集成 + 测量增量编译改善（GltfLoader/EditorPanel 拆分后）；
- B3：CMake 版本号与 CHANGELOG 统一（当前 project(VERSION 1.0.0) vs 0.17.12）。

### 4.9 编辑器与工具链

**现状**：ImGui 运行时覆盖层。面板：渲染统计/光照/相机/点光源/场景属性/动画/粒子/人物生成；Gizmo 平移+旋转；拾取；撤销重做（增删+属性手势）；中文 UI；响应式停靠是模拟的（imgui master 无 DockSpace）。

**演进路径（三步，不推倒重来）**：

| # | 动作 | 优先级 | 工作量 | 验收标准 |
|---|---|---|---|---|
| ED1 | **ImGui 升级 docking 分支**：删掉自造"边缘吸附"，获得真 DockSpace + 布局保存/恢复。成本极低收益极大 | P1 | 2 人日 | 布局可拖拽/保存/重置；`imgui.ini` 持久化 |
| ED2 | **编辑态/运行态分离（v0）**：Esc 暂停物理与脚本，编辑操作即时生效；Play 恢复。场景快照在切换时保存/恢复（复用 SceneSnapshot） | P1 | 3 人日 | 编辑中改属性→Play→Stop 属性还原 |
| ED3 | **资产浏览器**：列出 assets/ 与 .bhcache，双击 mesh 预览（复用现有渲染），拖入场景生成实体；图片/脚本显示类型图标 | P2 | 4 人日 | 拖入 glTF 在视口生成实例 |
| ED4 | **Prefab 工作流**：右键实体→"保存为 Prefab"；资产浏览器拖入实例化（依赖 E2） | P2 | 2 人日 | 人物 prefab 拖拽生成 |
| ED5 | **撤销覆盖补全**：Gizmo 已接、属性已接；补材质参数/光照参数/面板级操作入命令栈 | P2 | 2 人日 | 任意编辑操作 Ctrl+Z 均可回退 |
| ED6 | **快捷键体系 + 命令面板**：统一快捷键表（W/E/R 工具切换、Ctrl+S 保存、Ctrl+P Play）；Ctrl+K 模糊搜索命令（ImGui 简易实现） | P2 | 2 人日 | 快捷键速查卡（文档页） |
| ED7 | **性能剖析面板**：FrameProfiler（已有 CPU）+ GPU 时间戳（已有）合并为火焰图式 HUD；脚本耗时（J3）与物理耗时入列 | P2 | 2 人日 | 单面板看清一帧 CPU/GPU/脚本/物理分布 |
| ED8 | **崩溃恢复/自动保存**：编辑会话每 5 分钟自动保存 `.autosave`；启动检测残留提示恢复。配合 Windows SEH 崩溃转储（已有 dbghelp 链接） | P2 | 2 人日 | 手动 kill 进程后重启可恢复到 5 分钟内状态 |
| ED9 | **独立编辑器进程化、插件系统、Lua 编辑器扩展** | P3 | 10+ 人日 | 列入 P3 评估，先不做 |
| — | 编辑器性能目标：启动 ≤3s（当前秒级已达标，记录基线即可）；操作响应 <16ms；60fps @1080p 编辑态 | — | — | — |

### 4.10 跨平台与运行时

**现状**：Win/Linux/macOS/Android 四平台 CMake 预设；GLFW 桌面 + NativeActivity Android；平台抽象（Window/输入/文件）已具雏形；实机验证：**零**。

| # | 动作 | 优先级 | 工作量 | 验收标准 |
|---|---|---|---|---|
| X1 | **CI 真渲染**（与 §10 P0-1 同项）：Linux job 在 xvfb + lavapipe 下 `--screenshot` 真跑首帧，上传 PNG 产物 | P0 | 1 人日 | CI 日志有真实 vkQueueSubmit；注入 layout 缺陷 CI 变红 |
| X2 | **macOS(MoltenVK) CI 渲染**：GitHub macOS runner 装 MoltenVK 跑同款截图冒烟（重点：R16G16B16A16_SFLOAT HDR 附件与 renderPass 重建时序——本轮改动集中区） | P1 | 1 人日 | CI 产物三平台截图非全黑 |
| X3 | **Android 真机冒烟脚本**：写一份 30 分钟手动测试清单（触摸映射/CSM/后处理档位/截图），有真机时执行并记录帧率数据 | P1 | 0.5 人日 | 数据表入 docs/platform/android.md |
| X4 | **iOS**：P3 评估（MoltenVK 复用理论上顺路，但签名/资产/生命周期工作量 + 无真机测试条件下，先不做承诺） | P3 | — | — |
| X5 | **平台抽象补全**：文件系统（已有 PathUtils）、线程（std 封装已够）、剪贴板/拖放导入（GLFW 直用）、崩溃收集（Win 已有，Linux core dump 文档化） | P2 | 3 人日 | 拖放 .gltf 到窗口导入（编辑器入口） |
| — | 平台合规/商店 SDK/成就/云存档 | P3 | — | 随商业化阶段（§9.4）再评估 |

### 4.11 AI 与云原生

**现状**：NavGrid A*（规则网格）+ NavAgent 巡逻；无行为树/NavMesh/ML；云能力无（CI 除外）。

| # | 动作 | 优先级 | 工作量 | 验收标准 |
|---|---|---|---|---|
| AI1 | **行为树（纯逻辑核心）**：Sequence/Selector/Decorator/Action + 黑板。与 NavAgent/动画状态机联动示例（巡逻→发现玩家→追击） | P2 | 4 人日 | 单测全绿 + 编辑器可视化树状绘制（ImGui tree） |
| AI2 | **NavMesh**：集成 Recast（FetchContent）替代规则网格；NavGrid 保留为降级。数据经 bhcook 烘焙 | P2 | 6 人日 | 示例场景：任意多边形障碍物寻路（规则网格做不到的凹形） |
| AI3 | **GOAP / ML 推理** | P3 | — | 不承诺 |
| C1 | **云构建**：GitHub Actions 免费额度内做好缓存（ccache + FetchContent 缓存），矩阵全绿徽章 | P1 | 1 人日 | README 徽章真实反映状态 |
| C2 | **遥测/崩溃收集（引擎内）**：**默认关闭**的可选 opt-in 遥测（崩溃 minidump + 版本信息），本地文件优先。商业阶段再接云端 | P3 | 3 人日 | 隐私文档同步（§9.6） |

---

## 5. 核心模块改造清单（总表）

> 汇总 §4 全部动作 + §7 工程化项，按优先级与依赖排序。工作量单位：人日（单人）。

| ID | 模块 | 动作 | 优先级 | 工作量 | 依赖 |
|---|---|---|---|---|---|
| P0-1 | CI | xvfb + --screenshot 真渲染 + PNG 产物 + 负向验证 | **P0** | 1 | headless/screenshot 已就绪 |
| P0-2 | 工程 | upstream 配置 + 推送 21 提交 | **P0** | 0.1 | — |
| P0-3 | 渲染 | --exposure 参数 + 曝光线性验收 | **P0** | 0.5 | P0-1 |
| P0-4 | 文档 | README:319/UPGRADE_PLAN 收益表述/main.cpp:120 帮助文本三处修正 | **P0** | 0.3 | — |
| P0-5 | 发布 | tag v0.18.0 + LICENSE + Release 流水线激活 | **P0** | 0.5 | P0-2 |
| P0-6 | 工程 | 本机安装 Khronos 验证层 | **P0** | 0.2 | — |
| P1-1 | 工程提交纪律 | 当前 v0.17.12 WIP 拆分提交入库 | **P0** | 0.2 | — |
| P1-2 | samples | **垂直切片**：1000+ 实体/多数静止/真实父子层级的小场景（可沿用人物生成做主角） | P1 | 5 | P0-1 |
| P1-3 | CI | 成像回归基线（感知比对） | P1 | 2 | P1-2 |
| P1-4 | 架构 | 包投影移除（消费者逐个迁移） | P1 | 8 | — |
| P1-5 | 架构 | GltfLoader/EditorPanel 拆分（≤600 行/文件）+ 增量编译测量 | P1 | 3 | — |
| P1-6 | 工程 | thirdparty submodule/FetchContent 化 | P1 | 1 | — |
| P1-7 | 编辑器 | ImGui docking 升级 | P1 | 2 | — |
| P1-8 | 物理 | 固定时间步 + 插值 | P1 | 2 | — |
| P1-9 | 音频 | 3D 空间化 + 总线混音 | P1 | 4 | — |
| P1-10 | 跨平台 | macOS CI 渲染 + Android 冒烟清单 | P1 | 1.5 | P0-1 |
| P1-11 | 相机 | pitch 硬夹改地面碰撞约束 + 边界单测（D10） | P1 | 1 | — |
| P1-12 | 工程 | ccache + VK_LOD_CLAMP_NONE 4 处闭环（D12） | P1 | 1 | — |
| P2-1 | 脚本 | ScriptHost + Lua API v0 + 热重载 + 沙箱熔断 | P2 | 10 | E1 部分完成 |
| P2-2 | 资产 | bhcook + .bha 格式 + GUID + 异步加载 | P2 | 13 | P1-5 |
| P2-3 | 场景 | Prefab + 序列化版本迁移 | P2 | 6 | P2-2 |
| P2-4 | 编辑器 | 编辑/运行态分离 + 资产浏览器 + Prefab 流 + 命令面板 + 崩溃恢复 | P2 | 12 | P1-7, P2-3 |
| P2-5 | 渲染 | 材质系统统一 + Bindless + 变体缓存 + 移动档预设 | P2 | 13 | — |
| P2-6 | 动画 | 事件 + 根运动 + 混合树 + 压缩 | P2 | 12 | P2-2 |
| P2-7 | AI | 行为树 + Recast NavMesh | P2 | 10 | P2-2 |
| P2-8 | 音频 | DSP + 流式 + 资产化 | P2 | 5 | P2-2 |
| P2-9 | 工程 | 目录重构（§7.2）+ 分层规则强制 + 测试体系升级 | P2 | 8 | P1-6 |
| P2-10 | 生态 | 开源发布：GitHub public + 文档站 + 12 篇教程 + 首门课程 | P2 | 15（内容为主） | P1-2, P0-5 |
| P3 | 全部 | §4 各 P3 项：GPU-driven、Web 评估、iOS 评估、网络最小闭环、Mod/DLC、插件 SDK、遥测 | P3 | 按需 | P2 完成 |

**P0+P1 合计 ≈ 30 人日（约 6–8 周业余时间）；P2 合计 ≈ 105 人日（约 4–5 个月业余时间）。**

---

## 6. 内容与生态拓展方案

### 6.1 内置内容（P2 起）

| 内容 | 形态 | 数量目标（12 个月） |
|---|---|---|
| 示例项目 | samples/：① 垂直切片（角色+物理+脚本+粒子）；② 图形特性漫游（每个渲染特性的开关对比）；③ Lua 脚本入门最小项目 | 3 个 |
| 模板 | 新项目模板（CMake + 空 ECS 场景 + 一个脚本实体） | 1 个 |
| 教程 | 文档站系列："从零读懂一个 Vulkan 引擎"12 篇（与 CHANGELOG 复盘素材直接复用） | 12 篇 |
| 学习路径 | ① 图形初学者（对着引擎学 Vulkan）② 进阶（架构/性能）③ 用引擎做小游戏 | 3 条 |
| 最佳实践 | CHANGELOG 里的失败复盘精选（"三步回滚""VUID 审计法"）提炼为方法论页 | 5 篇 |

### 6.2 市场与扩展生态（P3，社区成立后）

- **插件 SDK（P3）**：编辑器面板插件（ImGui 面板注册 API）+ 资产导入器插件（bhcook 步骤注册）+ 脚本模块注册。接口先以"内置功能自己也走这套接口"的方式孵化（吃自己的狗粮）。
- **资产/模板市场**：**24 个月内不建平台**。用 GitHub 生态替代：awesome-bighero 列表 + 模板仓库标签。等月活用户 >500 再评估自建。
- **社区体系**：① GitHub Issues/Discussions（官方主阵地）；② 中文：知识星球或 QQ 群（低成本起步）；③ 英文：Discord 服务器；④ Game Jam：社区 >200 人后组织一次"用 BigHero 做 48 小时游戏"。
- **生态激励**：社区贡献者致谢墙 + 优秀示例官方收录 + 未来 Pro 版本对核心贡献者免费授权（远期）。

---

## 7. 代码质量与工程化方案

### 7.1 代码审查发现（本次审计新增 + 第二轮评估遗留汇总）

| # | 问题 | 位置 | 严重度 | 处置 |
|---|---|---|---|---|
| 1 | CI 不渲染帧，最高频缺陷类（首帧崩溃）全部落在盲区 | ci.yml:155, Application.cpp ValidateOnly() | **P0** | §5 P0-1 |
| 2 | 21 提交未推送 + 无 upstream，全部关键修复未经远端验证 | git 状态 | **P0** | §5 P0-2 |
| 3 | 双版本号：CMake `VERSION 1.0.0` vs CHANGELOG `0.17.12` | CMakeLists:4 | P0 | 统一为 0.x SemVer，CMake 读 CHANGELOG 头 |
| 4 | 无 LICENSE 文件；HOMEPAGE_URL 占位符 | CMakeLists:7 | P0 | §5 P0-5 |
| 5 | Application.cpp 1,473 行持续膨胀；GltfLoader.h 1,208 行 header-only（自带 JSON 解析器）；EditorPanel.h 1,059 行 header-only | src/ | P1 | §5 P1-5；JSON 解析器独立为 core/Json.h |
| 6 | SceneObject 包投影每帧双向全量往返（架构债根因） | Application.cpp:148,150 | P1 | §5 P1-4 |
| 7 | 相机 pitch 硬夹牺牲视角自由度，验证只有"测试全绿" | Camera.h（c1433f5） | P1 | §5 P1-11 |
| 8 | `SetParent` 生产调用 0 处，层级缓存收益无生产证据 | EcsScene.h:191 | P1 | 随 P1-2 切片与 P1-4 消除 |
| 9 | thirdparty 占 89.7% 跟踪文件 | thirdparty/ | P1 | §5 P1-6 |
| 10 | 双命名空间（BigHero:: / bighero::）迁移未完成，新代码规则靠文档约定 | 全仓 | P2 | §7.2 目录重构时一并收敛 |
| 11 | GBuffer position R16F 精度（±500m 地面 50m 处误差 2.4cm）未重评估 | 0.17.7 遗留 D14 | P2 | 实测远处 SSAO/SSR 后决定 camera-relative rendering |
| 12 | 序列化无版本号；无 GUID（路径引用易断链） | SceneSerializer.h | P2 | §5 P2-3 |
| 13 | 线程安全：JobSystem 存在但主循环单线程；物理/渲染/脚本共享 EcsScene 无明确线程模型约定 | 架构 | P2 | 明确"单写者"约定写入架构文档；JobSystem 仅用于资产加载/烘焙（P2-2 落地时兑现） |
| 14 | 内存：RAII 覆盖好、MemoryPools 统一、ASan CI 在跑——**此项现状良好**，维持 | — | — | 继续现有纪律 |

### 7.2 目录结构与分层架构（P2-9 重构目标形态）

```
BigHeroGameEngine/
├── engine/                 # 引擎库 BigHero::Engine（唯一可安装目标）
│   ├── core/               # 基础库：数学/容器/内存/日志/Json/序列化基元（bighero:: 命名空间）
│   ├── platform/           # 窗口/输入/文件/剪贴板抽象 + 各后端子目录
│   ├── render/             # RHI(Context/Buffer/Image/Pipeline) + RenderGraph + 特性 pass
│   ├── scene/              # EcsScene/Transform/加载器（加载器 .cpp 化）
│   ├── anim/ physics/ audio/   # 各系统（独立目录便于按需裁剪）
│   ├── script/             # ScriptHost + Lua 绑定（新增）
│   └── asset/              # AssetRegistry/GUID/.bha 读取/异步加载（新增）
├── editor/                 # 编辑器应用（独立 target，依赖 engine；engine 不得反向依赖）
├── runtime/                # 游戏 runtime 壳（发布产物用：engine + 用户资产，无编辑器）
├── tools/bhcook/           # 资产烘焙 CLI（新增）
├── samples/                # 垂直切片与示例（新增，各为独立 target）
├── tests/                  # 单测（engine 级 + samples 冒烟）
├── docs/                   # 文档源（文档站生成）
├── shaders/ assets/        # 引擎自带资产
└── thirdparty/             # 仅 .gitmodules + CMake 胶水，源码全部 FetchContent
```

**分层依赖规则（用 CMake target 可见性强制，不靠自觉）**：

```
core ← platform ← {render, scene, anim, physics, audio, script, asset} ← {editor, runtime, tools, samples, tests}
```

- 禁止反向 include：engine 内部模块不得 include editor/、samples/；
- editor 不进引擎安装产物（`install(TARGETS BigHeroEngine)` 只含 engine）；
- `target_link_libraries` 可见性 PRIVATE 化收敛（当前全 PUBLIC 是编译时间债）；
- clang-tidy `WarningsAsErrors` 先在 core/、game/、script/ 纯逻辑模块开启（UPGRADE_PLAN P3-4 既有项）。

**迁移步骤（不改行为的纯重构，每步独立可构建）**：
1. thirdparty 边界（P1-6）→ 2. 巨型文件拆分（P1-5）→ 3. 包投影移除（P1-4）→ 4. 目录重排（git mv 为主，P2-9）→ 5. 链接可见性收敛 → 6. 双命名空间收敛建议：`bighero::` 为准，`BigHero::` 保留别名逐步废弃（遵守 §7.3 弃用周期）。

### 7.3 API 稳定性策略（商业化前置件——用户代码要能活过版本升级）

| 级别 | 标记方式 | 变更承诺 | 典型对象 |
|---|---|---|---|
| **Experimental** | 命名空间 `bh::experimental::` 或 `// BH_EXPERIMENTAL` | 任意 minor 版本可破坏，无需迁移期 | GPU-driven、新渲染 pass |
| **Stable** | 默认 | minor 内不破坏；破坏必须走弃用期 | EcsScene/Transform/ScriptHost/asset API |
| **Deprecated** | `[[deprecated("since 0.19, removal in 0.21")]]` + CHANGELOG 迁移条目 | 至少存活 2 个 minor + 提供迁移指南 | 例：`BigHero::` 命名空间、SceneObject 投影 API |

**版本节奏与语义**：
- 0.x 阶段：**月度 minor**（0.18, 0.19, …），minor 允许破坏但必须：CHANGELOG 完整记录 + 迁移说明 + `BH_LOG_DEPRECATED` 运行时警告；
- 1.0 冻结条件（明确列出，防止永远 0.x）：脚本 API + 资产格式 + 场景序列化三者 Stable 连续 3 个 minor 无破坏性变更、文档站 API 参考覆盖 100% Stable API、两个真实外部项目各跑通一次跨版本升级；
- SemVer 与 tag：`v0.18.0` 格式，CMake/CHANGELOG/脚本 `bh.api_version` 三处同源生成。

### 7.4 测试体系升级（现状 104/2499 基础上）

| 层级 | 现状 | 动作 | 目标 |
|---|---|---|---|
| 单元测试 | 自研框架，14 文件，纯逻辑覆盖好 | 新模块（script/asset/anim 扩展）沿用同框架；覆盖率不追指标但**每个渲染修复必配回归测试**（现有惯例成文） | 200+ 用例 |
| 集成测试 | HeadCheck 自包含检查 | headless 模式跑"加载示例场景→步进 300 帧→断言无崩溃无 ERROR 日志" | 每平台 1 条 |
| 端到端/成像 | ❌ | P1-3 感知比对基线（2 场景 × 3 渲染路径） | 6 张基线 PNG 守护 |
| 性能回归 | 无 | FixedBenchmark：固定种子场景 300 帧记录帧时间 P50/P99，超阈值 CI 警告（不阻塞，先积累数据） | 基线数据表 |
| 模糊测试 | 无 | P3：.bha/MsgPack/gltf 解析器喂随机字节（libFuzzer 或简单随机循环），**损坏文件必须拒绝加载不崩溃**（T2 的 CRC 防线） | 3 个解析器 |

### 7.5 CI/CD 升级

| 项 | 现状 | 动作 |
|---|---|---|
| 触发 | push（但 main 未推送=实际空转） | P0-2 修复后生效；PR 也触发 |
| 校验 | `--validate-only` 文件存在性 | P0-1 替换为 xvfb 截图；validate-only 保留为快速前置 |
| 产物 | package job 死代码 | tag 触发 → Windows zip + Linux tar.gz 上传 Release |
| 缓存 | 无 | ccache + FetchContent 缓存（动作 C1） |
| 崩溃收集 | 无 | P3 遥测（C2）；CI 失败时上传 ASan 日志与截图产物（调试效率） |
| 发布自动化 | 无 | tag → CHANGELOG 提取该版本段落 → GitHub Release body 自动生成 |

---

## 8. 产品体验精致化方案（开发者旅程 8 步）

> 逐段审视目标用户从接触到产出的完整路径。现状一列基于实际代码路径；动作一列标注 §5 ID。

| 旅程步骤 | 现状（用户视角） | 目标体验 | 动作 |
|---|---|---|---|
| ① 发现 | README 44KB 特性清单，无一句话价值主张，无截图/视频 | README 首屏：一句话定位 + 3 张成品截图 + 60s GIF + 5 分钟快速开始；特性细节下沉 docs/ | P2-10 |
| ② 安装 | 需自装 Vulkan SDK/CMake/VS + 手动编译（对学习者可接受甚至是卖点；对使用者是门槛） | 双轨：学习者=源码构建文档（现成）；使用者=Release 二进制 zip（Win/Linux） | P0-5, C1 |
| ③ 创建项目 | 无概念：启动即 demo 场景 | `BigHeroGameEngine --new-project MyGame`（拷模板）；或编辑器内新建 | P2-4 |
| ④ 导入资产 | 手动放 assets/ 目录，重启生效 | 资产浏览器 + 拖放导入 + bhcook 自动烘焙 | P2-2, P2-4, X5 |
| ⑤ 搭建场景 | 面板按钮加立方体；Gizmo 平移/旋转 | + 缩放 Gizmo、多选、对齐吸附、Prefab 拖入 | P2-4 |
| ⑥ 写逻辑 | 改 C++ 重编译 5–15 分钟 | Lua 脚本热重载 1 秒内生效；错误定位到行 | P2-1 |
| ⑦ 调试 | FPS 面板 + GPU 时间戳 | 统一剖析 HUD（CPU/GPU/脚本/物理）；脚本错误面板；Play/Stop 状态隔离 | P1-7, P2-4, J4 |
| ⑧ 构建/发布 | 无打包能力 | `--package`：收集依赖资产 + 运行时壳 → 输出发布目录（本地优先，DLC/差分走 P3） | P2-2 延伸 |

**交互细节打磨清单（P2 编辑器改造时随做）**：批量删除/复制多选、拖拽对齐网格吸附、属性输入框拖拽微调（ImGui 已支持）、搜索过滤（资产浏览器/命令面板内置）、自动保存（ED8）、崩溃恢复（ED8）、中文 UI 保持 + 预留英文翻译表（i18n：字符串收口到 `strings_zh.h` 单文件，成本 1 人日，P2）。

**空状态与错误提示**：资产缺失时不静默（当前部分路径静默剔除），面板给黄色警告列表"3 个资产加载失败：路径/原因"；首启动无资产时显示引导卡片。

**用户反馈与迭代机制（P2-10 随建）**：GitHub Discussions 反馈区 + 模板（Bug 报告含引擎版本/系统/GPU/复现步骤）；版本内嵌 `--version` 输出构建信息（当前无）；崩溃 minidump 本地保存路径文档化。

---

## 9. 商业化推进方案

### 9.1 商业模式选型

| 模式 | 适配度 | 结论 |
|---|---|---|
| 收入分成（Unity/UE 式） | ✗ | 引擎未成熟，分成模式吓退早期用户 |
| 买断制引擎授权 | ✗ | 单人引擎无品牌溢价 |
| **开源核心 + 教育产品**（课程/电子书/训练营） | ✓✓✓ | 与现有资产 100% 匹配；学习者是最早真实客群；"教你造引擎"比"卖你引擎"可信 100 倍 |
| **赞助/会员**（GitHub Sponsors、爱发电） | ✓✓ | 零边际成本，积累核心粉丝 |
| **Pro 工具买断**（材质编辑器/Profiling Pro 等编辑器增强，社区版免费） | ✓（P3） | 对标 Rider/独立工具定价习惯 |
| **企业支持订阅**（工单响应/定制/培训） | ✓（P3） | 依赖项目知名度与稳定性 |
| 资产市场抽成 | ✗（24 月内） | 无市场 |

**开源协议决策**：**MIT**（而非 GPL 双许可）。理由：① 教学定位的最大诉求是"可以被任何用途阅读、摘抄、fork"，Copyleft 会吓跑教育用户与公司读者；② 双许可（GPL+商业）增加谈判与合规成本，单人无力运营；③ 收入不靠引擎代码本身，靠围绕它的产品与服务——MIT 下收入来源（课程/服务）完全不受影响。资产/课程内容用 CC BY-NC-SA 单独控制。

### 9.2 版本矩阵与定价（人民币，国内优先）

| 版本 | 内容 | 定价 | 上线时点 |
|---|---|---|---|
| 社区版（MIT 开源） | 引擎全部源码、示例、文档 | 免费 | P0（tag 起） |
| 电子书《从零实现现代 Vulkan 游戏引擎》 | 12 章，随引擎版本更新 2 年 | ¥199（首发 ¥129） | P2 末 |
| 系统视频课 | 30+ 小时：渲染管线逐 pass 拆解 + 架构复盘 + 实战改引擎 | ¥699（早鸟 ¥399） | P3 初 |
| Pro 工具包（买断） | 材质节点编辑器、剖析 Pro、一年 Pro 功能更新 | ¥299–499 | P3 |
| 企业支持 | 年度订阅：私有 issue 渠道、SLA 响应、2 次定制咨询 | ¥30,000–80,000/年 | P3 |
| 教育授权 | 高校课程包：课件 + 实验指导 + 师资答疑 | ¥5,000–15,000/校/年 | P3 |
| 赞助 | GitHub Sponsors / 爱发电 档位 ¥25/¥99/¥299 月 | — | P1 即可开（有内容有更新就有资格） |

### 9.3 收入测算（三情景，Year 1–3，人民币）

**关键假设**：GitHub 公开后 12 个月内 stars 800–3,000（同类教学型引擎中位）；课程转化率 = stars 的 1–2%；国内为主。

| 收入源 | 悲观 Y1 | 中性 Y1 | 中性 Y2 | 中性 Y3 |
|---|---|---|---|---|
| 赞助 | ¥3,000 | ¥10,000 | ¥25,000 | ¥40,000 |
| 电子书（Y2 起计中性 150 本/年） | — | — | ¥30,000 | ¥40,000 |
| 视频课 | — | — | ¥90,000（150 人×¥599） | ¥140,000 |
| 企业支持 | — | — | — | ¥60,000（1 单） |
| 教育授权 | — | — | — | ¥20,000 |
| **合计** | **¥3,000** | **¥10,000** | **¥145,000** | **¥300,000** |

> 诚实结论：**Year 1–2 的钱不多，这是定位"增长飞轮前置投入"的原因**。真正的价值曲线在 Y3：如果社区成立（月活 500+），教育+服务年收入 30 万级、加上企业定制可触达 50–100 万级——这已是单人维护的开源引擎在国内的现实上限。若目标是全职收入，需在 Y2 末复盘数据后决定是否引入第二人（见 §11）。

### 9.4 商业化基础设施（按需分批建，不提前建设）

| 设施 | 时点 | 最小实现 |
|---|---|---|
| 账号/身份 | P2 | GitHub 账号体系即够，不建自有账号 |
| 支付 | P2（电子书）/ P3（课程） | 国内：小鹅通/知识星球（自带支付+交付）；国际：Gumroad/Lemon Squeezy。**不自建支付/发票/退款系统** |
| License 管理 | P3（Pro 工具） | 买断制 + 离线激活码（Ed25519 签名离线校验，实现 <200 行，无需服务器）——不搞浮动许可/在线激活（用户群规模不值得） |
| 订单/发票 | P3 | 平台代开；企业合同走个人工作室主体（需注册个体户，成本 ~¥0） |
| 遥测 | P3 | opt-in 崩溃上报（C2），隐私政策同步 |

### 9.5 增长闭环

```
获客：GitHub Explore/B站引擎开发系列视频（把教程做成公开视频，课程是完整版）/
      知乎"手写 Vulkan 引擎"长文 / learnvulkan 类英文资源反向引流
  ↓
激活：README 快速开始 5 分钟跑通 → samples 垂直切片 → 改第一个 Lua 脚本看到效果
  ↓
留存：月度 minor 版本 + CHANGELOG 复盘（独特内容，别的引擎没有）+ Discussions 答疑
  ↓
转化：免费教程 → 电子书/课程（"想要完整推导过程？"）
  ↓
传播：学习者把引擎当作品集/论文素材 → 自发 stars/引用 → Game Jam 作品展示
```

**关键动作**：① B 站/YouTube 每月 1 支"引擎内部原理"视频（素材=现有 CHANGELOG 复盘，制作成本每支 1 人日）；② 每篇教程末尾放课程入口；③ 版本发布固定节奏制造回访。

### 9.6 合规清单

| 项 | 时点 | 说明 |
|---|---|---|
| LICENSE（MIT） | **P0** | 根目录 LICENSE + CMake HOMEPAGE_URL 修正 |
| 第三方许可合规 | P1 | thirdparty 各库许可（zlib/MIT/Apache）汇总页 NOTICE；ReactPhysics3D 是 zlib——确认无冲突 |
| 用户协议/隐私政策 | P3（遥测/商业上线时） | 遥测 opt-in 声明、课程平台隐私由平台承载 |
| 数据安全 | — | 无用户数据收集（P3 前），风险低 |
| 出口管制/知识产权 | — | 全自研代码（第三方经标准渠道引入）；代码含"BigHero"商标排查（P3，注册前） |
| 开源协议兼容 | P1 核查一次 | 引擎 MIT 输出 + 依赖均宽松许可，无 GPL 传染项（nlohmann MIT、glfw zlib、glm MIT、stb 公共域/MIT、miniaudio MIT-0、imgui MIT、RP3D zlib）——**已核查通过，写成 NOTICE 即可** |

---

## 10. 分阶段实施路线图

### Phase 0 —— 工程止血（第 1–2 周，≈2 人日）

| # | 任务 | 验收标准 |
|---|---|---|
| P0-2 | upstream + 推送 21 提交 | `git log origin/main..HEAD` 为空；远端 7 job 全绿 |
| P0-1 | CI 换 xvfb + `--screenshot` 真渲染，上传 PNG | 两张 PP on/off PNG 可下载非全黑；**注入一处 layout 缺陷验证 CI 变红** |
| P0-6 | 本机装 Khronos 验证层 | `vulkaninfo` 列出 validation layer |
| P0-3 | `--exposure` 参数 + 三档扫描 | 亮度线性数据入 CHANGELOG |
| P0-4 | 三处不实表述修正（README:319 / UPGRADE_PLAN 收益表述 / main.cpp:120） | 每句能力宣称可被一条命令复现 |
| P0-5 | v0.17.12 收尾提交 → tag `v0.18.0` → LICENSE + Release 流水线激活 | GitHub Release 出现 Win/Linux 产物 |
| P1-1 | WIP（PersonHost 等）拆分提交入库 | 工作区干净 |

**里程碑 M0：远端 CI 首次渲染真实帧 + 第一个 Release 发布。**

### Phase 1 —— 架构收敛与垂直切片（第 3–8 周，≈27 人日）

P1-2 垂直切片（5）→ P1-3 成像回归（2）→ P1-4 包投影移除（8）→ P1-5 巨型文件拆分（3）→ P1-6 thirdparty 边界（1）→ P1-7 ImGui docking（2）→ P1-8 固定步长物理（2）→ P1-9 音频 3D+总线（4）→ P1-10/11/12（3.5）。

**里程碑 M1（约第 8 周）：samples/ 垂直切片在 CI 每次提交被渲染守护；`BuildPacket` 从代码库消失；v0.19 发布。**

### Phase 2 —— 产品化：脚本 + 资产 + 编辑器 + 开源发布（第 9–26 周，≈105 人日）

按依赖序：**P2-2 资产管线（13）→ P2-1 脚本（10）→ P2-3 Prefab/版本迁移（6）→ P2-4 编辑器四件套（12）→ P2-5 渲染收尾（13）→ P2-6 动画（12）→ P2-7 AI（10）→ P2-8 音频补全（5）→ P2-9 工程重构（8）**，其中 **P2-10 生态发布（文档站/教程/开源宣传，15）与开发并行**，在 P2-1/P2-2/P2-4 就绪后立即执行。

**里程碑 M2（约第 20 周）**：v0.21 发布——Lua 脚本 + 烘焙管线 + 资产浏览器/Prefab 编辑器；**里程碑 M3（约第 26 周）**：GitHub 公开 + 文档站 + 12 篇教程 + 电子书首发。**这是商业化的发射点。**

### Phase 3 —— 生态与商业化（第 27 周起，持续）

- 第 27–40 周：视频课 + Pro 工具 v1（材质编辑器优先级最高，源自用户投票）+ 企业支持渠道开张 + P3 技术项按社区需求排序（GPU-driven / Mod / 遥测 / Web 评估）；
- 第 40 周后：**复盘三问**——月活用户？月收入？每周社区 issue 量？据答案决定：全职化 / 引入第二维护者 / 维持副业节奏。

---

## 11. 资源与团队配置建议

**现状：单人**。以下按阶段给最小配置（不虚构团队需求）：

| 阶段 | 配置 | 说明 |
|---|---|---|
| P0–P1 | **1 人（作者）** | 无需任何增员；纪律比人头重要 |
| P2 | 1 人 + **1 名兼职内容合伙人**（可选，分成制） | 文档站/教程/B站视频制作约占 P2 投入的 15%，若作者不擅长或不耐烦做内容，用"电子书/课程收入分成 30%"换内容产能 |
| P2 | 硬件缺口：**需补 1 台 macOS 实机**（Mac mini M 系列最低配即可，≈¥4,000 一次性）——MoltenVK 验证无法靠 CI 完全覆盖，而 macOS 是三平台承诺的一部分 | Linux 可用 CI+lavapipe 过渡 |
| P3 | 1 人 + 社区核心贡献者 2–3 名（自然生长，不招人）+ 兼职内容 | 若中性情景收入兑现（Y2 ¥145K），可支撑作者半全职；全职化门槛建议定为稳定月收入 ¥15K |

**成本估算（24 个月）**：硬件 ¥4,000；域名+文档站托管 ≈¥600/年（GitHub Pages 免费，仅域名）；课程平台分成 ≈ 收入的 20–35%；合计现金成本 <¥10,000——**这个项目的商业化成本结构极轻，最大投入永远是作者时间**。

---

## 12. 风险与应对策略

| # | 风险 | 概率 | 影响 | 应对 |
|---|---|---|---|---|
| R1 | **单人带宽不足，范围蔓延**（本方案 P2 有 105 人日，是最大风险源） | 高 | 高 | §3.3 不做清单硬约束；每阶段结束做"砍一半"复盘；P2 各项可独立降级（如混合树可退化为简单 crossfade） |
| R2 | **教育产品卖不动**（内容营销是作者不擅长的可能性存在） | 中 | 中 | 先电子书后视频课（制作成本低 10×）；B 站视频先验证流量再投入课程制作；P2-10 延后不影响技术路线 |
| R3 | Godot 4.x 持续碾压通用场景 | 确定 | 低 | 不竞争通用场景；价值锚点在"可读性/教学"，与 Godot 无重叠伤害 |
| R4 | Vulkan-only 限制受众（mac 需 MoltenVK、无 Web） | 确定 | 中 | 接受：这正是"学习价值"定位的一部分；Web 列 P3 评估 |
| R5 | 热情周期中断（个人项目的第一死因） | 中 | 高 | 月度小版本节奏制造正反馈；社区（哪怕 5 人）是最好的坚持外因；文档化程度高=中断后可低成本恢复 |
| R6 | 脚本/资产系统破坏现有稳定性 | 中 | 中 | 全部新模块纯逻辑核心单测化（沿用现有架构惯例）；feature flag 渐进启用 |
| R7 | 开源后劣质 fork/抄袭课程的困扰 | 低 | 低 | MIT 下这是生态繁荣的代价而非损失；课程服务的信任不可复制 |
| R8 | 收入不及预期且作者需要收入 | 未知 | 高 | 方案设计的收入路径是"锦上添花"型：即使零收入，引擎+文档资产仍在增值；全职化决策有明确门槛（¥15K/月）而非赌注 |
| R9 | 三平台实机（尤其 macOS）出现平台特定缺陷 | 中 | 中 | X2 CI 渲染尽早暴露；README 平台支持矩阵如实标注"实验性" |

---

## 13. 关键指标与验收标准

### 13.1 技术 KPI（每季度复核）

| 指标 | 现状（2026-09-19） | Q4 2026 目标 | 2027 中目标 |
|---|---|---|---|
| CI 真实渲染帧 | ❌（0 帧） | ✅ 每次提交 2 张截图 | 6 基线成像回归 |
| 未推送提交积压 | 21 | 0（常态 ≤1） | 0 |
| git tag / Release | 0 | 1（v0.18.0） | 6+ |
| 单测 | 104 / 2,499 | 130 / 3,000+ | 200+ |
| Application.cpp 行数 | 1,473 | ≤900 | ≤600 |
| header-only 巨型文件 | 2（1,208/1,059 行） | 0（全部拆分） | 0 |
| 包投影代码量 | 全量往返 | -50% | 0（删除） |
| 三平台 CI 渲染产物 | 0 平台 | Linux | Win+Linux+macOS |
| 示例项目 | 0 | 1（垂直切片） | 3 |
| 脚本系统 | ❌ | — | ✅ Lua API Stable |
| 资产管线 | ❌（运行时直解源格式） | — | ✅ bhcook + .bha + GUID |
| 性能基线 | 无记录 | FixedBenchmark 基线表 | 三档 GPU 数据齐全 |

### 13.2 增长与商业 KPI

| 指标 | P2 末（M3）目标 | 2027 中目标 | 2027 末目标 |
|---|---|---|---|
| GitHub stars | 300–800 | 1,500 | 3,000 |
| 文档站月访问 | 2,000 | 8,000 | 15,000 |
| 教程阅读完成率（有数据后） | — | ≥40% | ≥40% |
| 电子书销量（累计） | 首发 50 | 200 | 400 |
| 视频课学员 | — | 150 | 400 |
| 赞助者月经常额 MRR | ¥300 | ¥1,500 | ¥3,000 |
| 社区月活（Discussions/群） | 50 | 200 | 500 |
| 企业支持客户 | — | 0–1 | 1–2 |
| 收入累计 | ¥5,000 | ¥100,000 | ¥300,000 |

**验收原则**：每个 KPI 都必须"一条命令/一个页面"可查证——沿用本项目"宣称必须可复现"的纪律。

---

## 14. 下一步可立即执行的动作清单（本周）

按顺序执行，总耗时 ≈1.5 人日，全部来自第二轮评估已验证结论，无技术风险：

1. **提交当前 WIP**（0.5h）：PersonHost/FirstPersonCamera/人物面板拆成 2–3 个 conventional commit（`feat(scene): 人物生成系统…`、`feat(camera): 第一人称…`）。
2. **配置 upstream 并推送**（0.1h）：`git branch --set-upstream-to=origin/main main && git push`——然后确认远端 7 job 结果，任何红项都是真问题情报。
3. **本机安装 Vulkan 验证层**（0.2h）：Vulkan SDK 已有则 `vulkaninfo` 确认 `VK_LAYER_KHRONOS_validation`，确保开发期布局失配即时报错。
4. **CI 改真渲染**（2h）：`ci.yml:155` 步骤替换为 `xvfb-run -a ./BigHeroGameEngine --post-process --screenshot out/ci_pp.png`（+ 一跑不带 `--post-process`），`actions/upload-artifact` 上传；随后**故意删掉 TAA 历史图一处 layout 迁移提交验证 CI 变红，再还原**——这条负向验证是防线有效性的唯一证明。
5. **修正三处文档**（0.5h）：README:319 删"首帧验证"（或改为指向第 4 步的新现实）；UPGRADE_PLAN "收益恢复"改为"消除每帧容器重建开销；增量矩阵收益待垂直切片验证"；main.cpp:120 帮助文本对齐实现。
6. **补 `--exposure` 参数**（2h）：接到 `renderer_.SetExposure`，CI 中三档截图，亮度线性数据替换 CHANGELOG 里含糊的 "75.9 vs 70.2"。
7. **发布**（1h）：根目录加 MIT `LICENSE`；CMakeLists 版本改 0.18.0 并修正 HOMEPAGE_URL；`git tag v0.18.0 && git push --tags`；确认 GitHub Release 自动产出 Win/Linux 包。
8. **（可选，当天可完成）开 GitHub Sponsors / 爱发电**：有 tag、有 Release、有 CHANGELOG 连续记录后即具备"值得被赞助"的最小形态。

**之后的第一周开始**：垂直切片 samples/（P1-2）——它同时是成像回归基线的载体、层级缓存收益的验证场景、未来教程的素材、课程产品的 demo。这是全方案里复利最高的单一动作。

---

## 附：本方案与既有文档的关系

- 《项目评估与开发计划_2026-09-19.md》（第二轮评估）的 P0-1~5 与 P1 系列已**全部吸收**进本方案 Phase 0/1，未推翻任何结论；
- 《UPGRADE_PLAN.md》的 P2/P3 未执行项（RenderGraph 跨队列屏障、FrameProfiler HUD、GBuffer 精度）并入本方案 §4/§7 对应位置，UPGRADE_PLAN 建议改造为"纯技术债看板"，战略层以本方案为准；
- 本方案 §3.3 不做清单与 §5 工作量估计是**新增加的约束层**——此前文档只有"做什么"，没有"明确不做什么"和"做多久"，这是单人项目能走完路线的关键补充。
