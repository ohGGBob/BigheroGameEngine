# BigHeroGameEngine 全面评估与开发计划（第二轮）

评估日期：2026-09-19（14:23）｜ 评估对象：`D:\BigheroGameEngine`（HEAD = `c1433f5`，135 提交）
上轮基线：同日 HEAD = `75e1ecd`（124 提交），评分 9.0 ｜ **本轮间隔新增 11 个提交**
评估方式：静态审计 + 本地实测（测试套件实跑、git 状态核验、生产调用链追踪、着色器逐点核对）

---

## 一、总体结论

**综合评分：9.3 / 10**（上轮同日 9.0，↑0.3）

上轮标记的两个最实质缺陷**都已真正修好**，而且修的方式正是我建议的路径：

- **P0-3 双重色调映射**：严格按 CHANGELOG 0.17.9 记下的四条教训，拆成**三个可独立回滚的 commit**（`258fa72` HDR 附件升级 → `155842a` 前向/天空线性化 → `005bf56` 延迟链收敛）。我逐点核对了四个着色器，三链确实统一到"离屏图保持线性 HDR，链路末端唯一一次 ×exposure+ACES"。顺带还挖出并修掉了 PP 开启路径的 3 个既有缺陷（TAA 历史图首帧必崩的 layout 迁移缺失、描述符池容量不足、`SetPostProcessing` 重建顺序颠倒导致悬垂视图段错误）。
- **P0-4 层级缓存收益归零**：`SyncFromPacket` 改为逐实体差异比较（`EcsScene.h:293-306`），并配了针对性回归测试 `EcsScene.SyncFromPacketCacheStaysClean`，用 `RecomputeWorld()` 返回值断言重建计数（预热 5 → 等值 round-trip **0** → 单节点移动 **1** → TRS 写回 5 → 自转增量 5）。**这个测试设计是对的**——它断言的是重算次数而非仅仅"结果正确"，能真正锁死退化。

另有两项超预期的自足建设：**内置截图能力**（`--screenshot`，走完整 `Run()` 路径渲染 30 帧后读回交换链成像落盘 PNG）和 headless 段错误修复（`ddf79aa`，此前 headless 下 `ImGui_ImplGlfw_InitForVulkan(NULL)` 直接 exit=139，**CI/校验路径完全不可用**）。后者尤其关键——它把"让 CI 真跑首帧"的技术障碍清除了。

**但仍有三点让我没有给到 9.5+：**

1. **P0-1（CI 真校验）仍未做，而它现在已经没有技术障碍了**。`ValidateOnly()`（`Application.cpp:66-115`）依旧只做 `std::filesystem::exists()` 检查 25 个 `.spv`；CI L155 依旧跑 `--headless --validate-only`。**讽刺的是，刚建好的 `--screenshot` 正好能填这个缺口，却还没接上。**
2. **README 新增了一处不实表述**：L319「Linux Debug 额外在 lavapipe 下跑 headless **首帧验证**」——该步骤从不渲染任何一帧。这是本轮文档同步时新引入的，属"修一处、错一处"。
3. **"收益恢复"的宣称超出了实际边界**（见 D1，本轮最重要的技术发现）。

**一句话定性**：项目的**缺陷修复能力和工程诚实度已经过关**（三步可回滚、失败复盘、VUID 为纲、回归测试断言到重算次数），现在的短板集中且单一——**验证防线仍停在文件存在性检查，20 个提交（含全部关键修复）从未经过远端 CI**。

---

## 二、本轮实测结果（可复现）

| 核验项 | 方法 | 结果 |
|---|---|---|
| 单元测试 | 实跑 `out/build/x64-Release/Release/BigHeroTests.exe` | **98 registered / 98 ran / 2438 check(s) / 0 failure(s)** ✅ 与 README、CHANGELOG、UPGRADE_PLAN 三处完全一致 |
| 新增回归测试 | 读 `test_ecs_scene.cpp` | `SyncFromPacketCacheStaysClean` 存在，断言重建计数（0/1/5/5/0/5）✅ 验证深度合格 |
| 着色器三链统一 | 逐点 grep `acesFilm`/`outputTarget`/`tonemapDirect`/`exposure` | `frag.glsl:354` 按 `outputTarget` 分支；`skybox.frag.glsl:55` 按 `tonemapDirect` 分支；`deferred_light.frag.glsl` 已**无** acesFilm；`deferred_composite.frag.glsl:41` 唯一 ACES ✅ |
| 层级缓存置脏点 | 读 `EcsScene.h:285-330` | 已改为 `if (t.position != o.position \|\| ...)` 条件置脏 ✅ |
| 增量路径 | 读 `EcsScene.h:162-186` + `Transform.h:252-256` | `UpdateSpins` 走 `SetLocalMatrix` → `MarkDirty` 逐节点，**不触发** `hierarchyDirty_` ✅ |
| CI job 数 | 解析 `jobs:` 段 | **7 个**（windows/linux/linux-sanitizers/android/macos/lint/package）。**更正上轮口径**：上轮我误把 workflow 触发器 `on: push` 算成 job，报了"8 job"；README 现写 7 job 是准确的 |
| CI 无头校验命令 | 读 `.github/workflows/ci.yml:147-155` | 仍为 `--headless --validate-only` ⚠️ **未渲染任何帧** |
| `ValidateOnly()` 实现 | 读 `Application.cpp:66-115` | 仍只做 `filesystem::exists()` × 25 ⚠️ **P0-1 未落地** |
| 帮助文本 | `main.cpp:120` | 仍写 `--validate-only    Initialize Vulkan + pipelines and exit` ⚠️ **与实现不符（沿用上轮 D5，未修）** |
| 截图能力执行路径 | 读 `main.cpp:110-138` + `Application.cpp:283-322` | `--screenshot` 走**完整 `Run()`**（真实录制+提交 30 帧后回读落盘）✅ 这正是 CI 缺的东西 |
| headless 截图降级 | 读 `Renderer.cpp:786-797` | headless 无交换链 → WARN 并忽略（`screenshotSucceeded_=false`）⚠️ 故 CI 须走 xvfb 而非 `--headless` |
| 推送状态 | `git log origin/main..HEAD` | **20 个提交未推送**（上轮 9 个，本轮又积 11 个）⚠️ **P0-2 未做** |
| upstream | `git rev-parse --abbrev-ref '@{u}'` | `fatal: no upstream configured` ⚠️ **未配置，这正是积压成因** |
| 版本标签 | `git tag` | **仍为空** ⚠️ **P1-1 未做**，`package` job 仍是死代码 |
| 垂直切片 | 检查 `samples/` + CMake target | **无 `samples/` 目录**；仅 3 个 target（引擎/HeaderCheck/Tests）⚠️ **P1-2 未做** |
| `SetParent` 生产用例 | grep 全 `src/` | **仍 0 处生产调用**（仅 `EcsScene.h:191`、`Transform.h:244/259` 三处定义 + 测试）⚠️ |

> **未能实测的项（诚实标注）**：真机 GPU 渲染表现、PP on/off 成像的实际暗部亮度、曝光滑条线性度、三平台实机运行、远端 CI 通过状态。以下涉及画面与运行时的判断均为静态分析，须真机复验。

---

## 三、优点

### O1.（新增）缺陷修复的执行纪律已达专业水准
0.17.10 的双重色调映射修复是范本级的：严格按上一轮失败复盘写下的四条教训，**把"先保证描述符布局合法"与"再改色彩域"拆成独立 commit**，每步各自冒烟验证、各自可回滚。这正是 0.17.9 整体回退时最缺的东西。三个 commit 还顺带清掉了 PP 开启路径上潜伏的三个既有缺陷（`Image::TransitionLayout` 缺 `UNDEFINED→SHADER_READ_ONLY` 分支导致 TAA 历史图首帧必崩、后处理描述符池 40/1/16 容量不足、`SetPostProcessing` 先建离屏 FB 后重建帧资源导致悬垂视图段错误）——**说明修复过程是真在跑，而不是改完就提交**。

### O2.（新增）主动建设自足验证工具，而非依赖外部手段
0.17.10 明确记下"验收依赖外部截图工具……引擎无截图能力，需人工在 PP 开启状态截图"，**下一个 commit（`0184dfe`）就把截图能力做进了引擎**。这个反应模式很健康：识别验证短板 → 补工具 → 而不是绕过去。

实现也内行：在 `vkQueueSubmit` 之后、`vkQueuePresentKHR` 之前读回，layout 走完整 `PRESENT_SRC → TRANSFER_SRC → copy → 恢复 PRESENT_SRC`，处理了交换链 B8G8R8A8 的 R/B 通道交换，headless 无交换链时优雅降级。**PP 开/关的最终画面（含 UI 覆盖层）都在这一处收敛**，所以它天然适合做 CI 的成像回归基线。

### O3.（新增）回归测试断言到了"机制层"而非仅"结果层"
`SyncFromPacketCacheStaysClean` 用 `RecomputeWorld()` 返回的重算节点数做断言，覆盖五种情形（预热全量 / 等值 round-trip 零重建 / 增量单节点 / TRS 写回全量 / 自转增量）。**这类断言能锁死"收益归零"这种结果正确但机制退化的缺陷**——恰好是上轮 D2 的形态。测试文件注释还写明了"D2 回归"与修复前后对比，可追溯性好。

### O4. 渲染特性广度与技术选型（沿用上轮，仍是最强项）
延迟+前向双路径、CSM 4 级联 2×2 图集（纹素对齐、级间渐变、PCF 子块收拢）、点光源立方体阴影、IBL 全套分裂求和、SSAO/SSR、TAA（Halton(2,3) + YCoCg AABB 钳制）、体积雾（HG 相函数 + 抖动）、God Rays（复用 CSM 图集做遮挡）、自动曝光（三级亮度金字塔 + ping-pong 指数适应）、DoF、运动模糊、ASC CDL 风格色调分级。**本轮 HDR 附件升级后，色彩管线的一致性比上轮更好了。**

### O5. 规范符合性与失败复盘的诚实度（沿用上轮）
以 VUID 编号为纲做全仓穷举审计；0.17.9-reverted 条目完整记录失败尝试与四条教训；0.17.6 明确写「**诚实的边界**：本节为静态定位，未在 780M 上运行验证」；0.17.6 主动标注 4 处 `VK_LOD_CLAMP_NONE` 同类隐患并说明"本次不改，避免扩大改动面"。本轮 `dbed9d4` 又是一例——发现 PP 开启时那组 LDR 帧缓冲"休眠不兼容"（附件格式与 SFLOAT 通道失配且从不被消费），直接改为条件创建，并用日志实证 `framebuffers=3 / 0`。

### O6. CI 矩阵与纯逻辑核心可测试性（沿用上轮）
7 job 五平台矩阵（Windows VS2022 ×2 / Linux ×2 / macOS ×2 / Android NDK 编译校验 / ASan+UBSan / clang-format + clang-tidy / tag 触发打包）。A\* 导航、粒子、命令栈、骨骼蒙皮、动画混合、Gizmo 数学、GpuAllocator（注入 `CreateBlockFn` 解耦真实 `vkAllocateMemory`）、RenderGraph 别名屏障全部无 GPU 可离线单测——这是 2438 断言能在无显卡环境全绿的原因。

### O7. UPGRADE_PLAN 自订的防退化条款开始发挥作用
第 4 条要求"性能类模块必须附带生产接线 commit 才算完成"，本轮 UPGRADE_PLAN 确实补记了「**生产接线**：已接入渲染路径 7 处调用……0.17.10 改为逐实体差异比较后……」。**条款从"写了没用"变成"真的在被引用"**，这是治理层面的进步（尽管表述仍有过度宣称，见 D1）。

---

## 四、缺点

### 【P0 级】

#### D1.（本轮最重要的技术发现）"70× 收益恢复"的宣称超出了实际边界
修复本身是真实有效的，但**它省下的不是矩阵计算，而是容器重建**。必须区分清楚：

**修复前**（每帧）：`SyncFromPacket` 无条件置 `hierarchyDirty_=true` → `EnsureWorld()` 走重建分支：分配 2 个 `std::vector` + 构建 `std::unordered_map<uint32_t,int32_t>`（`EcsScene.h:369-391`）+ `ResetMatrices` 内部再 `assign` 4 个 vector + `BuildChildren()` 重建邻接表（`Transform.h:200-210`、`315-325`）→ 然后 `ForceRecomputeAll()` 算全部矩阵。

**修复后**（每帧）：`EnsureWorld()` 干净 → **O(1) 返回引用** ✅；但 `UpdateSpins` 仍对每个自转实体调 `SetLocalMatrix` → `MarkDirty` → `UpdateWorld()` 重算所有脏子树。

**关键在于**：默认场景（`Scene.h:50` 的 `BuildDefaultScene`）6 个物体的 `spinSpeed` 分别是 `30 / -18 / 55 / 42 / 10 / 24`——**全部非零**。所以每帧 6 个节点全是脏根，矩阵该算还是一个不少地算。新增测试自己也印证了这点：`world.UpdateSpins(1.0f); CHECK(world.RecomputeWorld() == 5)`，注释写明「本场景全部实体有自转速度 → **逐实体增量重算**」。

**准确结论**：
- ✅ **真实收益**：消除了每帧的 vector 分配 + unordered_map 构建 + `BuildChildren` 邻接表重建 + `ResetMatrices` 的 4 次 assign。这是实打实的内存与 CPU 开销削减，规模越大越明显。
- ❌ **未兑现收益**：70× 基准的前提是"每帧只改 1 节点 / 10k 节点"。**当前场景所有物体都在自转，增量重算的收益仍为零**。
- ⚠️ **文档过度宣称**：UPGRADE_PLAN 现写「修复 `SyncFromPacket` 每帧全量置脏后**收益恢复**」，README/CHANGELOG 沿用。准确表述应是「消除每帧容器重建开销；增量矩阵收益需待存在静止实体或父子层级的真实场景」。
- ⚠️ **`SetParent` 生产仍 0 调用**：父子级联这一缓存的核心目标场景，在生产中依然不存在。

这不是要否定修复——修复是必要且正确的第一步。但**"收益恢复"这四个字需要数据支撑**，而当前唯一的数据（70× 基准）测的不是这个场景。

#### D2.（上轮 D1，仍未修，但障碍已清除）CI 无头校验仍不渲染任何一帧
`Application.cpp:66-115` 的 `ValidateOnly()` 逐字未变：25 个 `filesystem::exists()` 检查，然后 `return EXIT_SUCCESS`。CI L155 仍跑 `--headless --validate-only`。

**本轮的变化是：修复它的两个前置障碍都被清除了。**
- `ddf79aa` 修掉了 headless 下 `EditorOverlay::Init` 的段错误（此前 exit=139，headless 渲染路径**完全不可用**），CHANGELOG 明写这是"headless 渲染路径（CI/校验）"的阻塞项；
- `0184dfe` 的 `--screenshot` 走**完整 `Run()`**，会真实录制并提交 30 帧、回读成像。

**所以现在没有理由继续让 CI 空跑。** 而后果依旧严重：0.17.9 那类"描述符布局与图像 layout 失配导致首帧 DEVICE_LOST"，以及本轮 `155842a` 修掉的"TAA 历史图 `UNDEFINED→SHADER_READ_ONLY` 迁移缺失首帧必崩"——**CI 在结构上都不可能捕获**，因为它从不走到首帧。本机又无 Khronos 验证层。**项目最危险的一类缺陷，恰好全落在唯一自动化防线的盲区里，而且这类缺陷本轮又出现了两次。**

#### D3.（新增）README 引入了不实表述
`README.md:319`：「Linux Debug 额外在 lavapipe（软件 Vulkan）下跑 headless **首帧验证**」。

该步骤不渲染任何帧（见 D2）。本轮文档同步（`b43e8a7`）修正了测试数据和 CI job 数，却**新引入了这处更严重的夸大**——上轮 README 至少只说"自动编译"（低估），现在是声称做了实际没做的验证（高估）。**低估自己浪费信用，高估自己会误导决策**，后者更危险。

#### D4.（上轮 D4，恶化）20 个提交未推送，全部关键修复从未经过 CI
`origin/main` 仍停在 `307255d`，本地领先 **20 个提交**（上轮 9 个）。未推送的内容包括本轮**全部**修复：三步色调映射、层级缓存差异比较、HDR 附件升级、两个崩溃修复、截图能力、相机修复。

`main` 分支**仍未配置 upstream**（`git rev-parse '@{u}'` → fatal），这是积压的直接成因——每次都要 `git push origin main` 显式指定，摩擦足够让人一直拖着。

**后果**：Linux / macOS / Android / ASan / UBSan / clang-tidy **一次都没跑过这些改动**。特别是 `155842a` 改了 HDR 附件格式与 renderPass 重建时序、`258fa72` 改了主通道存储格式——这类改动在 MoltenVK 和移动 GPU 上的行为与 Windows/AMD 差异很大。

#### D5.（上轮 D3，代码已修但验收不完整）P0-3 的验收标准未真正达成
着色器三链统一已确认落地（O1）。但上轮定的验收标准是两条：**暗部亮度恢复** + **曝光滑条线性生效**。

CHANGELOG 0.17.11 给出的验证是：`--post-process --screenshot pp_on.png` 与 `--screenshot pp_off.png` 双跑，"亮度均值 75.9 vs 70.2，差异符合双重色调映射预期"。

**这不足以验收**：
- 75.9 vs 70.2 只是 PP 开/关两个状态的对比，**没有与修复前基线对比**，无法证明"暗部亮度恢复"；
- **完全没做曝光滑条扫描**——而"曝光滑条平方生效"正是该缺陷最易观测的症状，也是上轮验收标准明确列出的一条；
- 措辞"差异符合双重色调映射预期"含糊，读起来像双重映射仍存在。

0.17.10 自己写了"验收依赖外部截图工具对比暗部亮度与曝光滑条线性"，0.17.11 把工具建好了，**却只用了它的一半**。曝光线性验证需要能命令行设定 exposure（当前 `--exposure` 参数不存在，只有运行时滑条 + `renderer_.SetExposure`）。

### 【P1 级】

#### D6.（上轮 D9，未做，且已成为最大缺口）无垂直切片 / 无可玩 demo
仍无 `samples/` 目录，`src/` 下只有 `main.cpp` 一个入口。引擎当前形态是"技术演示台"（6 个自转物体 + glTF 模型 + 粒子 + 角色控制器）。

**本轮 D1 让这个缺口的代价更清楚了**：正因为没有一个"上千实体、部分静止、带父子层级"的真实场景，才无法回答"增量缓存到底值多少"，也才会出现"收益恢复"这种只能靠基准测试而非真实帧计时来支撑的宣称。**垂直切片是 D1 类问题的唯一系统性解法。**

#### D7.（上轮 D8，未做）无 tag，release 流水线仍是死代码
`git tag` 为空 → `package` job（`if: startsWith(github.ref, 'refs/tags/')`）从未触发。CHANGELOG 已写到 0.17.11，仓库里连 `v0.17.8` 都不存在。**这是本轮成本最低的一项**（一条 `git tag` + `git push --tags`）。

#### D8.（上轮 D6，未做）上帝对象残留
| 文件 | 体积 | 问题 |
|---|---|---|
| `src/app/Application.cpp` | 1,226 行（本轮又 +30） | 拆分后仍是最大单文件；主循环 + 6 个 Record* + 场景同步 + 截图控制 |
| `src/render/PostProcessor.cpp` | 52 KB → 本轮 +102/-? | 11 个后处理通道全在一个 TU，本轮 HDR 改动集中在此 |
| `src/render/Renderer.cpp` | 本轮 +155 | 截图回读逻辑新增于此，持续膨胀 |
| `src/scene/GltfLoader.h` | 48 KB **header-only** | 自带 JSON 解析器 + glTF 全解析 + 骨骼 + 动画 |
| `src/editor/EditorPanel.h` | 46 KB **header-only** | 全部 ImGui 面板逻辑 |

两个巨型 header-only 文件仍是增量编译时间的主要拖累源。

#### D9.（上轮 D7，未做）仓库体积构成失衡
`thirdparty/`（glfw + imgui + glm + stb）仍占 **2,338 / 2,606 = 89.7%** 跟踪文件。imgui 用 master 快照、缺 DockSpace API，故自造"边缘吸附 + 响应式重排"模拟停靠（技术债）。UPGRADE_PLAN P3 已列未执行。

#### D10.（新增，中等）相机 pitch 修复治标且牺牲了视角自由度
`c1433f5` 把 `OrbitCamera::Orbit` 的 `pitch_` 下限从 `-0.1f`（约 -6°）收紧为 `0.0f`，解决远距拉满时相机钻入地面（`y ≈ 0.5 - 4.0 = -3.5`）导致全黑。

根因分析是准确的（提交信息里给了算式）。但**修复方式是收窄输入域，而非解决遮挡**：
- 副作用：**永久失去俯视地平线以下的视角**。对轨道相机而言，轻微负 pitch（看地面/物体底部）是常见需求；
- 未治本：真正的问题是相机与场景几何无碰撞/裁剪处理。若 `target_y` 更低、或 `maxDistance` 更大、或场景加高物体，同类遮挡会以别的形式复现；
- 该修复的验证只有"98/98 全绿"——单元测试测不到相机视角语义，**属于典型的"测试通过但未验证行为"**。

建议改为相机-地面碰撞约束（限制 `y ≥ 地面高度 + 余量`）而非硬夹 pitch，或至少加一条断言 pitch 边界语义的单元测试。

### 【P2 级】

#### D11.（上轮 D10，未做）三平台实机渲染验证仍缺
Linux(X11/Wayland)、macOS(MoltenVK)、Android 真机——CI 只保证编译（Android job 名为 `compile check`）。**本轮改了主通道附件格式（LDR→R16G16B16A16_SFLOAT）与 renderPass 重建时序，这两处在 MoltenVK 上的行为尤其需要验证。**

#### D12.（上轮 D11，未闭环）`VK_LOD_CLAMP_NONE` 遗留项无归属
CHANGELOG 0.17.6 标注的 4 处同类隐患（`SSAO.cpp` / `SSR.cpp` / `Renderer_PostFx.cpp` / `descriptor_set.h`）仍写着"列为后续核查项，本次不改"。**跨了两轮没有归属**。

#### D13.（上轮 D5 残留）帮助文本仍与实现不符
`main.cpp:120` 的 `--validate-only    Initialize Vulkan + pipelines and exit` 依旧描述着不存在的实现。本轮修了 README 的测试数据，漏了这处（且新增了 D3 那处错误）。

#### D14. GBuffer position 精度权衡未评估
地面扩至 ±500m 后，R16G16B16A16_SFLOAT 在 50m 处重建误差约 2.4cm 且随距离线性增长（0.17.7 已标注）。本轮 HDR 附件升级动了色彩附件，**深度/位置附件精度未重新评估**，远处 SSAO/SSR artifacts 未实测。

---

## 五、进一步开发计划

> **核心判断变化**：上轮我说"停止加功能，先造垂直切片"。本轮修正为——**先用 3 个极低成本动作把验证防线补上（P0-1/2/3，合计不到一天工作量），再造垂直切片**。理由：验证防线现在是空的，而它恰好是垂直切片能发挥价值的前提（切片跑起来后需要 CI 帮你守住成像不退化）。而补齐防线的成本，因为 `ddf79aa` + `0184dfe` 已经铺好路，降到了极低。

### P0 —— 补上验证防线（成本极低，建议立即执行）

| # | 任务 | 具体做法 | 验收标准 |
|---|---|---|---|
| **P0-1** | **CI 改用 `--screenshot` 真跑首帧**（最高性价比） | 把 `ci.yml:147-155` 那步从 `--headless --validate-only` 改为在 **xvfb 下不加 `--headless`** 运行：`xvfb-run -a ./BigHeroGameEngine --post-process --screenshot out/ci_pp.png`，再加一跑 `--screenshot out/ci_nopp.png`。原因：`Renderer.cpp:786` 在 headless 下会忽略截图（无交换链），必须走 xvfb 真实窗口路径才能录制+提交帧。用 `actions/upload-artifact` 上传两张 PNG。`--validate-only` 可保留为前置快速文件检查，或连同 `ValidateOnly()` 一起删除 | ① CI 日志出现真实的帧渲染与 `vkQueueSubmit`；② 两张 PNG 产物可下载且非全黑；③ **负向验证：故意注入一处 layout 失配（如把 TAA 历史图的 `UNDEFINED→SHADER_READ_ONLY` 迁移删掉），确认 CI 变红**——这条必须做，否则无法证明防线有效 |
| **P0-2** | **配置 upstream 并推送 20 个积压提交** | `git branch --set-upstream-to=origin/main main` → `git push` | `git log origin/main..HEAD` 为空；远端 7 job 全绿（或暴露出真问题——那也是收益）。**先配 upstream**，否则积压会第三次发生 |
| **P0-3** | **补齐 P0-3 的曝光线性验收** | 新增 `--exposure <f>` 命令行参数（接到现有 `renderer_.SetExposure`），用 P0-1 的截图能力跑 `--exposure 0.5 / 1.0 / 2.0` 三张图，计算亮度均值 | 三张图亮度均值**近似线性**（0.5→1.0 的增量 ≈ 1.0→2.0 的增量）；结果写进 CHANGELOG，替换掉当前含糊的"75.9 vs 70.2"表述 |
| **P0-4** | **修正三处不实/过度宣称表述** | ① `README.md:319` 删掉"首帧验证"（或按 P0-1 做完后改为准确描述）；② `UPGRADE_PLAN.md` 的"收益恢复"改为"消除每帧容器重建开销；增量矩阵收益待有静止/层级实体的真实场景验证"；③ `main.cpp:120` 帮助文本改为与实现一致 | 文档中每句能力宣称都能被一条命令复现；**无一处高估** |
| **P0-5** | **打第一个 tag，激活 release 流水线** | `git tag v0.17.11 && git push --tags` | GitHub Release 出现产物；`package` job 首次真实执行 |

### P1 —— 兑现收益与切片（建议 1-2 个迭代）

| # | 任务 | 具体做法 | 验收标准 |
|---|---|---|---|
| **P1-1** | **垂直切片 demo**（D1/D6 的系统性解法） | 新增 `samples/` 目录 + 独立 CMake target，做一个**小而完整**的场景，且必须满足三个此前缺失的条件：**① 上千实体；② 大部分静止（只有少数动）；③ 有真实父子层级**（glTF 骨架节点 / 编辑器父子编组，让 `SetParent` 在生产中被调用） | ① 连续运行 ≥10 分钟无崩溃；② **在该场景下实测 `RecomputeWorld()` 的每帧重算数 / 总实体数比值**，用真实帧计时给出层级缓存的收益数字，替换掉 70× 合成基准；③ 作为 CI 新增的成像回归目标（复用 P0-1 机制） |
| **P1-2** | **成像回归基线**（把截图能力变成防线） | 在 `samples/` 场景上固定相机位与种子，CI 每次跑截图并与签入的基线 PNG 做**感知差异比对**（如逐像素容差 + 差异像素占比阈值） | 引入一次故意的色彩管线退化（如把 `deferred_composite` 的 ACES 删掉）能让 CI 变红 |
| **P1-3** | **相机遮挡治本** | 把 `pitch_ ≥ 0` 的硬夹改为相机-地面碰撞约束（射线/解析求交，限制 `y ≥ 地面高度 + 余量`），恢复负 pitch 输入域；补一条断言 pitch 边界语义的单元测试 | 远距拉满不再全黑；**可俯视地平线以下**；新增单元测试覆盖 |
| **P1-4** | **拆分两个巨型 header-only 文件** | `GltfLoader.h`(48KB) → `JsonParser.h` + `GltfLoader.cpp`；`EditorPanel.h`(46KB) → 按面板分组拆 `.cpp` | 单文件 ≤600 行；**测量并记录**改一行后的增量重编时长改善 |
| **P1-5** | **第三方边界隔离** | glfw/imgui/glm/stb 改 submodule 或 FetchContent（ReactPhysics3D 已有现成范式） | 跟踪文件数 2,606 → ~300；CI 五平台仍全绿 |
| **P1-6** | **闭环 `VK_LOD_CLAMP_NONE` 4 处遗留项** | 核查 `SSAO.cpp`/`SSR.cpp`/`Renderer_PostFx.cpp`/`descriptor_set.h` | CHANGELOG 中该"待核查"转为"已核查"并写明结论 |

### P2 —— 实机验证（依赖硬件，可与 P1 并行）

| # | 任务 | 验收标准 |
|---|---|---|
| **P2-1** | **AMD 780M 真机回归**（`872fb9b` 明确标"待验证"，已搁 3 天；本轮又改了 HDR 附件格式，风险叠加） | headless 走完 HDR/立方图/辐照度/预滤波/BRDF/IBL 全流程无 DEVICE_LOST；小窗 960×540 存活 ≥30s；系统事件无新增 141/144；**PP 开/关 + 延迟三路径各截一张图** |
| **P2-2** | **本机装 Khronos 验证层**（一次性投入，长期收益） | `vulkaninfo` 列出 `VK_LAYER_KHRONOS_validation`；此后布局失配在开发期即时暴露，不再靠驱动 DEVICE_LOST 反推。**这是 D2 类缺陷反复出现的根源性对策** |
| **P2-3** | macOS(MoltenVK) 实机渲染 | **重点核查 HDR 附件（R16G16B16A16_SFLOAT）+ input attachment（延迟光照子通道）+ renderPass 重建时序**——本轮改动集中区；截图对比 Windows 基线 |
| **P2-4** | Linux(X11 + Wayland) 实机渲染 | 截图对比基线无差异 |
| **P2-5** | Android 真机冒烟 | 触摸→键鼠映射可用；CSM/后处理在移动 GPU 的帧率档位数据；建议提供"移动档"预设（关闭体积雾/God Rays/TAA） |

### P3 —— 架构收敛（长期，不阻塞发布）

| # | 任务 | 说明 |
|---|---|---|
| **P3-1** | **消除 SceneObject 包投影中间层** | 每帧 `SyncFromPacket` + `BuildPacket` 双向全量往返（`Application.cpp:148,150`）仍在。渲染已直读 ECS，但 Gizmo/拾取/撤销快照/序列化/物理关节锚点/编辑器面板仍消费包投影。**这是 D1 的根因**——只要包投影每帧往返，`SyncFromPacket` 就必须做差异比较来防御；彻底移除后这类问题不会再生。按消费者逐个迁移到直读 ECS，每迁一个删一段投影 |
| **P3-2** | ImGui 升级到含 DockSpace 的稳定版 | 移除自造的"边缘吸附 + 响应式重排"模拟停靠 |
| **P3-3** | RenderGraph 跨队列/所有权转移屏障推导 | UPGRADE_PLAN P2 已列未执行 |
| **P3-4** | clang-tidy `WarningsAsErrors` 渐进开启 | 建议先在 `core/`、`game/` 等纯逻辑模块开 |
| **P3-5** | FrameProfiler(core) 与 GPU 时间戳统一 HUD | UPGRADE_PLAN P2 已列 |
| **P3-6** | GBuffer position 精度评估（camera-relative rendering） | 见 D14；实测远处 SSAO/SSR artifacts 后决定 |

---

## 六、给项目方向的一条建议

**先花不到一天把 P0-1 做完——它是当前唯一能改变项目风险结构的事。**

理由很具体：本轮又出现了两次"首帧崩溃"类缺陷（`155842a` 修掉的 TAA 历史图 layout 迁移缺失、`258fa72` 涉及的 renderPass 重建时序），而 CI 依然一帧都不渲染。也就是说，**这类缺陷正在以每轮 1-2 个的频率产生，而唯一的自动化防线对它们完全失明**。同时，做这件事的两个前置障碍刚刚被自己清除了：`ddf79aa` 让 headless 不再段错误，`0184dfe` 让引擎能自足产出成像。工具已经在手上，只差把 CI 那一行的 `--headless --validate-only` 换成 xvfb 下的 `--screenshot`。

做完之后，D1（收益无法证实）、D5（曝光线性无法验收）、D6（切片无守护）三件事会同时获得解决手段——**它们缺的都是同一个东西：一个能自动跑起来、产出可对比成像的真实场景**。

至于"只建不接"的模式判断，本轮需要更新：它已经从"建了不接"（早期）、"接了但收益被架构抵消"（上轮），演进到本轮的"**修好了，但收益边界被宣称得比实测更宽**"。严重性在递减——这是好事，说明治理条款在起作用。但形态变了，核验方式也得跟着变：**光看是否接线、是否置脏已经不够，还要问"宣称收益的那个场景，在生产中存在吗"**（D1 的答案是：不存在，因为所有物体都在自转，且 `SetParent` 零调用）。

---

## 七、与上轮评估（同日 9.0，HEAD `75e1ecd`）的对照

| 上轮问题 | 本轮核验结果 |
|---|---|
| **D1** CI 无头校验名实不符，只查文件存在 | ⚠️ **未修**，`ValidateOnly()` 逐字未变；但 `ddf79aa` + `0184dfe` 已清除实施障碍，修复成本降到极低（P0-1） |
| **D2** 层级缓存收益被每帧置脏抵消，70× 不成立 | ✅ **已修**（`b59b8ab` 差异比较 + 回归测试断言重算次数）；🔶 **但宣称超出边界**——省下的是容器重建而非矩阵计算，默认场景 6 物体全部自转，增量矩阵收益仍为零，`SetParent` 生产仍 0 调用（本轮 D1） |
| **D3** 双重色调映射修复失败后搁置 | ✅ **已修**（三 commit 可回滚，着色器三链统一已逐点核对）；🔶 **验收不完整**——只做了 PP on/off 对比，未做曝光线性扫描（本轮 D5） |
| **D4** 9 提交未推送，main 无 upstream | ⚠️ **恶化**：20 提交未推送，upstream 仍未配置 |
| **D5** 文档与代码不同步 | 🔶 **部分修复**：README/UPGRADE_PLAN 测试数据已同步为 98/2438 ✅、CI job 数已写准 7 个 ✅；但**新引入** README L319"headless 首帧验证"不实表述，`main.cpp:120` 帮助文本仍未修 |
| **D6** 上帝对象残留 | ⚠️ 未做；`Application.cpp` 1226 行、`Renderer.cpp` 本轮 +155 行，仍在膨胀 |
| **D7** thirdparty 占 89.7% 跟踪文件 | ⚠️ 未做 |
| **D8** 无 tag，package job 从未触发 | ⚠️ 未做（P0-5，成本最低的一项） |
| **D9** 无垂直切片 demo | ⚠️ 未做；**本轮 D1 让该缺口的代价变得更具体**（P1-1） |
| **D10** 三平台实机渲染未验证 | ⚠️ 未做；**风险上升**——本轮改了主通道附件格式与 renderPass 重建时序 |
| **D11** `VK_LOD_CLAMP_NONE` 4 处遗留项无归属 | ⚠️ 跨两轮未闭环 |
| 上轮口径错误：CI "8 job" | ✅ **本轮更正**：实际 7 job，上轮误把 workflow 触发器 `on: push` 计入 |

**新增问题**：D3（README 不实表述）、D10（相机 pitch 修复治标且牺牲视角自由度，验证只有"测试全绿"）。

**新增优点**：O1（三步可回滚的修复纪律）、O2（主动建设自足验证工具）、O3（回归测试断言到机制层）。
